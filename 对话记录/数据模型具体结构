# 提示词数据模型结构

根据代码分析，提示词数据模型（任务2.1完成成果）具有以下结构：

## 核心数据类型

```typescript
interface Prompt {
  id: string;                // 唯一标识符，UUIDv4格式
  title: string;             // 提示词标题
  content: string;           // 提示词内容
  isFavorite: boolean;       // 是否收藏
  createdAt: number;         // 创建时间戳
  updatedAt: number;         // 更新时间戳
  useCount: number;          // 使用次数
  lastUsed: number;          // 最后使用时间
  tags?: string[];           // 标签数组（可选）
  source?: 'user' | 'optimize' | 'predefined';  // 来源
  category?: string;         // 分类（可选）
  isActive: boolean;         // 是否激活（用于软删除）
  optimizationHistory?: Array<{  // 优化历史（可选）
    version: number;         // 版本号
    content: string;         // 优化后内容
    timestamp: number;       // 优化时间
  }>
}
```

## 输入接口

### 创建提示词输入

```typescript
interface CreatePromptInput {
  title: string;             // 标题
  content: string;           // 内容
  isFavorite?: boolean;      // 是否收藏（可选）
  tags?: string[];           // 标签（可选）
  source?: 'user' | 'optimize' | 'predefined';  // 来源（可选）
  category?: string;         // 分类（可选）
}
```

### 更新提示词输入

```typescript
interface UpdatePromptInput {
  title?: string;            // 标题（可选）
  content?: string;          // 内容（可选）
  isFavorite?: boolean;      // 是否收藏（可选）
  updatedAt?: number;        // 更新时间（可选）
  tags?: string[];           // 标签（可选）
  category?: string;         // 分类（可选）
  isActive?: boolean;        // 是否激活（可选）
  optimizationHistory?: Array<{  // 优化历史（可选）
    version: number;
    content: string;
    timestamp: number;
  }>
}
```

### 过滤条件接口

```typescript
interface PromptFilter {
  searchTerm?: string;       // 搜索关键词
  category?: string;         // 分类筛选
  tags?: string[];           // 标签筛选
  onlyFavorites?: boolean;   // 仅收藏
  includeInactive?: boolean; // 包含已停用项目
  sortBy?: 'usage' | 'favorite' | 'time' | 'alphabetical';  // 排序方式
  offset?: number;           // 分页偏移量
  limit?: number;            // 每页限制数量
}
```

## 错误处理模型

```typescript
enum PromptErrorCode {
  VALIDATION_ERROR = 'validation_error',
  STORAGE_ERROR = 'storage_error',
  ITEM_NOT_FOUND = 'item_not_found',
  STORAGE_LIMIT_EXCEEDED = 'storage_limit_exceeded',
  IMPORT_ERROR = 'import_error',
  EXPORT_ERROR = 'export_error'
}

class PromptError extends Error {
  code: PromptErrorCode;
  field?: string;
  
  // 构造函数和静态方法用于创建特定类型的错误
}
```

## 存储限制常量

```typescript
const PROMPT_STORAGE_LIMITS = {
  LIGHTWEIGHT_MODE_MAX: 50   // 轻量模式最大存储数量
}
```

## 主要特性改进

1. **分页和增量加载**：通过`offset`和`limit`参数，允许前端分页加载数据
2. **结构化错误处理**：使用`PromptError`类和`PromptErrorCode`枚举，提供详细错误信息
3. **命名一致性**：使用`isFavorite`和`isActive`替代旧的`favorite`和`active`字段，保持布尔值字段命名一致性
4. **安全ID生成**：使用UUIDv4格式生成唯一ID，减少冲突风险
5. **数据验证**：在`validator.ts`中实现全面的数据验证逻辑

数据模型设计反映了现代TypeScript最佳实践，包括类型安全、接口分离和严格验证，同时保持了与旧数据的兼容性。
