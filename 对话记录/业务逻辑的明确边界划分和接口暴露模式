# 重新评估目录结构方案


## 优化后的目录结构方案

以下是修订后的目录结构，更强调业务逻辑与展示逻辑的分离：

```
extension/src/
├── assets/           # 静态资源
├── components/       # 所有UI组件(纯展示逻辑)
│   ├── common/       # 基础UI组件
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   └── ...
│   ├── library/      # 提示词库相关组件
│   ├── navigation/   # 导航相关组件
│   ├── optimize/     # 提示词优化相关组件
│   └── auth/         # 认证相关组件
├── services/         # 业务逻辑层(所有业务逻辑)
│   ├── prompt/       # 提示词相关业务逻辑
│   │   ├── index.ts  # 统一导出接口
│   │   ├── types.ts  # 类型定义
│   │   ├── actions.ts # 业务操作实现
│   │   └── utils.ts  # 相关工具函数
│   ├── auth/         # 认证相关业务逻辑
│   │   ├── index.ts
│   │   ├── types.ts
│   │   └── actions.ts
│   ├── optimize/     # 提示词优化相关业务逻辑
│   │   ├── index.ts
│   │   ├── types.ts
│   │   └── actions.ts
│   ├── storage/      # 存储相关业务逻辑(原storage.ts)
│   │   ├── index.ts
│   │   ├── types.ts
│   │   └── actions.ts
│   └── messaging/    # 消息通信相关业务逻辑(原messaging.ts)
│       ├── index.ts
│       ├── types.ts
│       └── actions.ts
├── hooks/            # 自定义钩子(连接UI和业务逻辑)
│   ├── usePrompts.ts
│   ├── useAuth.ts
│   ├── useOptimize.ts
│   └── ...
├── utils/            # 通用工具函数(非业务逻辑)
│   ├── formatting.ts
│   ├── validation.ts
│   └── ...
├── types/            # 全局类型定义
│   └── index.ts
├── pages/            # 页面入口点
│   ├── sidepanel/
│   └── sidebar/
├── content/          # 内容脚本
├── background/       # 背景脚本
├── App.tsx           # 主应用组件
├── main.tsx          # 应用入口
└── index.css         # 全局样式
```

## 优化说明

1. **分离业务逻辑和展示逻辑**：
   - 新增`services/`目录专门存放所有业务逻辑
   - 每个业务领域有自己的子目录，通过index.ts统一导出接口
   - 组件目录只包含UI展示逻辑，不包含业务逻辑

2. **接口暴露模式**：
   - 每个服务模块通过index.ts文件导出接口，隐藏实现细节
   - 例如：`services/prompt/index.ts`导出所有提示词相关功能
   - 使用方只需`import { addPrompt, getPrompts } from '../services/prompt'`

3. **清晰的职责划分**：
   - `components/`: 负责UI渲染和用户交互
   - `services/`: 负责业务逻辑和数据操作
   - `hooks/`: 负责连接UI组件和业务服务
   - `utils/`: 提供非业务相关的通用工具

4. **类型定义集中管理**：
   - 每个服务模块内有自己的types.ts定义领域类型
   - 全局共享类型放在`types/`目录

## 业务逻辑接口示例

```typescript
// services/prompt/index.ts - 接口导出
import { Prompt, PromptFilter } from './types';
import { addPrompt, getPrompts, updatePrompt, deletePrompt, searchPrompts } from './actions';

// 只导出接口，隐藏实现
export {
  // 类型
  Prompt,
  PromptFilter,
  
  // 操作方法
  addPrompt,
  getPrompts,
  updatePrompt,
  deletePrompt,
  searchPrompts
};
```

```typescript
// services/prompt/types.ts - 类型定义
export interface Prompt {
  id: string;
  content: string;
  createdAt: string;
  updatedAt: string;
  isFavorite: boolean;
  useCount: number;
  lastUsed: string;
}

export interface PromptFilter {
  searchTerm?: string;
  sortBy?: 'usage' | 'favorite' | 'time';
  limit?: number;
}
```

```typescript
// services/prompt/actions.ts - 实现
import { Prompt, PromptFilter } from './types';
import { getStorage, setStorage } from '../storage';

// 实现细节，不直接暴露
const STORAGE_KEY = 'prompts';

// 导出的接口方法
export async function getPrompts(filter?: PromptFilter): Promise<Prompt[]> {
  const prompts = await getStorage<Prompt[]>(STORAGE_KEY) || [];
  
  // 实现过滤、排序等逻辑
  return prompts;
}

export async function addPrompt(content: string): Promise<Prompt> {
  // 实现添加提示词逻辑
}
```

## 业务逻辑调用示例

```typescript
// hooks/usePrompts.ts - 连接UI和业务逻辑
import { useState, useEffect } from 'react';
import { Prompt, getPrompts, addPrompt } from '../services/prompt';

export function usePrompts() {
  const [prompts, setPrompts] = useState<Prompt[]>([]);
  const [loading, setLoading] = useState(true);
  
  useEffect(() => {
    const loadPrompts = async () => {
      setLoading(true);
      try {
        const data = await getPrompts();
        setPrompts(data);
      } catch (error) {
        console.error('Failed to load prompts:', error);
      } finally {
        setLoading(false);
      }
    };
    
    loadPrompts();
  }, []);
  
  const createPrompt = async (content: string) => {
    try {
      const newPrompt = await addPrompt(content);
      setPrompts(prev => [...prev, newPrompt]);
      return newPrompt;
    } catch (error) {
      console.error('Failed to create prompt:', error);
      throw error;
    }
  };
  
  return {
    prompts,
    loading,
    createPrompt
  };
}
```

```typescript
// components/library/PromptLibrary.tsx - 仅处理UI
import React from 'react';
import { usePrompts } from '../../hooks/usePrompts';
import { PromptCard } from './PromptCard';

export const PromptLibrary = () => {
  const { prompts, loading, createPrompt } = usePrompts();
  
  // 仅处理UI渲染和交互，不包含业务逻辑
  if (loading) return <div>加载中...</div>;
  
  return (
    <div className="prompt-library">
      {prompts.map(prompt => (
        <PromptCard key={prompt.id} prompt={prompt} />
      ))}
    </div>
  );
};
```

这个修改后的方案更好地符合您提出的标准，实现了业务逻辑的明确边界划分和接口暴露模式。请确认这个方案是否符合您的要求，确认后我将开始执行更改。
