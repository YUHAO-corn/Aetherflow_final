我会创建一份更完善的入门引导，专门解决重复造轮子和目录混乱的问题。

# 🔍 AetherFlow项目入门引导

## 学习路径建议

作为新加入的AI编程员，请按照以下步骤了解我们的项目：

1. **首先阅读产品需求和架构设计**：
   - 从 `架构文档-Aetherflow/11-产品需求文档.md` 开始，了解项目目标
   - 接着阅读 `架构文档-Aetherflow/00-项目实现说明.md` 了解整体架构

2. **了解用户流程**：
   - 查看 `架构文档-Aetherflow/12-用户流程文档.md` 了解用户如何使用我们的产品

3. **探索代码结构**：
   - 查看 `extension/src` 目录，了解核心代码组织

4. **浏览核心组件**：
   - 了解 `extension/src/components` 目录中的组件
   - 查看 `架构文档-Aetherflow/components.md` 了解组件设计文档

5. **了解任务清单**：
   - 查看 `架构文档-Aetherflow/51-任务清单.md` 了解当前需要完成的任务

## 💡 避免重复造轮子指南

在开发新功能前，请务必：

1. **检查现有组件库**：
   - 浏览 `extension/src/components/common` 目录下的所有可复用组件
   - 检查 `extension/src/components` 目录下是否有符合需求的业务组件
   - 查看 `extension/src/hooks` 目录下是否有可以复用的钩子函数

2. **检查现有服务**：
   - 浏览 `extension/src/services` 目录，了解已实现的业务逻辑
   - 检查特定服务的 `index.ts` 文件了解可用API接口

3. **检查工具函数**：
   - 检查 `extension/src/utils` 目录下是否有可复用的工具函数

4. **代码搜索**：
   - 使用代码搜索功能查找关键词，确认功能是否已实现
   - 搜索类似变量名或函数名，查看相似功能的实现方式

## 📁 正确使用目录结构

严格遵循项目的目录结构规范：

### 业务逻辑与UI分离原则
```
extension/src/
├── components/       # 所有UI组件(纯展示逻辑)
├── services/         # 业务逻辑层(所有业务逻辑)
├── hooks/            # 自定义钩子(连接UI和业务逻辑)
├── utils/            # 通用工具函数(非业务逻辑)
├── types/            # 全局类型定义
├── pages/            # 页面入口点
├── content/          # 内容脚本
└── background/       # 背景脚本
```

### 添加新文件指南

1. **新增组件**：
   - 通用UI组件 → `components/common/`
   - 特定功能组件 → `components/[功能域]/`（如`library`、`optimize`）
   - 使用PascalCase命名（如`Button.tsx`、`PromptCard.tsx`）

2. **新增业务逻辑**：
   - 先确认相关服务是否存在于 `services/[服务域]/`
   - 在现有服务中添加新函数，而非创建新服务
   - 服务对外导出应通过 `index.ts` 文件

3. **新增钩子函数**：
   - 放置于 `hooks/` 目录
   - 使用`use`前缀命名（如`usePrompts.ts`）

4. **新增工具函数**：
   - 放置于 `utils/` 目录，按功能类型组织

5. **新增类型定义**：
   - 业务相关类型 → 对应服务的 `types.ts`
   - 全局类型 → `types/` 目录

## 🚫 禁止事项

1. 不要在`src`根目录下直接添加新文件
3. 不要创建新的目录结构，除非经过团队讨论
4. 不要在组件中直接调用服务，应通过hooks层

## 🗂️ 关键文件导航

当你需要了解特定功能时，可以参考以下文件：

1. **入口文件**：
   - `extension/src/main.tsx` - 应用入口点
   - `extension/src/App.tsx` - 主应用组件

2. **配置文件**：
   - `extension/manifest.json` - Chrome扩展配置
   - `extension/package.json` - 项目依赖

3. **核心功能目录**：
   - `extension/src/background/` - 浏览器扩展后台脚本
   - `extension/src/content/` - 内容脚本
   - `extension/src/components/` - 可复用UI组件

如有任何疑问，请随时咨询。
