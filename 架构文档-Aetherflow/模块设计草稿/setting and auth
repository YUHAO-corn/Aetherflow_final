# AetherFlow 侧边栏界面元素设计

整体布局:
  结构: 
    - Header: 显示应用名称"AetherFlow"和等级信息
    - Navigation: 两个选项卡，分别是"提示词库"和"提示词优化"
    - Content: 根据选择的选项卡显示不同的内容
    - Footer: 包含"设置"按钮

组件设计:
  设置按钮:
    位置: 侧边栏的最底部，紧贴着内容区域的下方
    样式:
      类型: 扁平按钮，无明显边框或背景色
      内容: Lucide React的齿轮(Settings)图标 + 文字"设置"
      颜色: 
        默认: text-magic-400（柔和的灰色，与暗色主题协调）
        悬停: 
          背景: bg-magic-700/30（半透明深色）
          文字: text-magic-300
    交互:
      点击行为: 打开设置抽屉组件(SettingsDrawer)，从右侧滑入
      抽屉布局:
        位置: 从屏幕右侧滑入，占据屏幕右侧部分(宽度约80px)
        头部: 显示"设置"标题和关闭按钮(X图标)
        内容区域: 包含各项设置选项
      当前功能:
        - 导出所有提示词: 提供CSV格式导出功能
          - 按钮样式: 主题色背景，白色文字
          - 导出内容: 包含所有收藏提示词的标题、内容、创建时间、使用次数等信息
          - 导出格式: CSV文件
      关闭方式: 
        - 点击右上角关闭按钮(X图标)
        - 点击抽屉外区域

  认证按钮:
    位置: 侧边栏头部(Header)的右上角
    样式:
      类型: 扁平按钮，无明显边框
      颜色:
        默认: text-magic-200（较亮的灰色）
        悬停:
          背景: bg-magic-700/30
      文字显示:
        未登录状态: "登录"
        已登录状态: "个人中心"
    交互:
      未登录状态:
        点击行为: 打开认证抽屉组件(AuthDrawer)，从右侧滑入
        抽屉布局:
          位置: 从屏幕右侧滑入，占据屏幕右侧部分(宽度约80px)
          头部: 显示"登录/注册"标题和关闭按钮(X图标)
          内容区域: 包含登录和注册表单
        抽屉内容: 登录和注册选项，输入邮箱、密码等信息
        关闭方式: 
          - 点击右上角关闭按钮(X图标)
          - 点击抽屉外区域
      已登录状态:
        点击行为: 打开用户信息抽屉(UserProfileDrawer)，从右侧滑入
        抽屉布局:
          位置: 从屏幕右侧滑入，占据屏幕右侧部分(宽度约80px)
          头部: 显示"个人中心"标题和关闭按钮(X图标)
          内容区域: 显示用户信息和快捷操作
        抽屉内容: 
          - 用户信息: 用户名、邮箱等
          - 快捷操作: 查看收藏的提示词、使用记录、设置等
        关闭方式: 
          - 点击右上角关闭按钮(X图标)
          - 点击抽屉外区域

组件实现参考:
  抽屉组件: 参考PromptDetailDrawer.tsx的实现
  抽屉样式:
    - 位置: fixed inset-y-0 right-0
    - 宽度: w-80
    - 背景: bg-gradient-to-br from-magic-800 to-magic-900
    - 边框: border-l border-magic-700/30
    - 阴影: shadow-xl
    - z-index: z-30
    - 动画: transform transition-transform duration-300
    - 开关状态: ${isOpen ? 'translate-x-0' : 'translate-x-full'}