---
title: PromptShortcut 组件设计文档
version: 1.0.0
author: Aetherflow团队
---

# PromptShortcut 组件

## 概述
PromptShortcut 组件旨在提供一种无缝、沉浸式的提示词联想体验，让用户在主输入框内通过键入 "/" 触发，并直接在输入过程中完成提示词的选择和插入。

## 组件状态
- **初始状态**: 组件在未激活时，不显示任何额外的UI元素，完全融入主输入框
- **激活状态**: 当用户在主输入框中键入 "/" 时触发

## 交互逻辑
### 激活机制
- **触发方式**: 在主输入框中键入 "/"
- **取消方式**: 
  - 手动: 按下 "Esc" 键
  - 自动: 关键词长度超过4个字符仍未匹配时

### 联想搜索功能
- **实时响应**: 根据用户在 "/" 后输入的字符进行实时联想搜索
- **动态更新**: 联想结果根据输入实时更新显示在浮层中
- **搜索词变更监听**: 当用户删除或修改搜索词时，立即触发新搜索并更新结果
- **搜索显示规则**:
  - 未输入文字时: 推荐10条提示词，按相关性排序
  - 有输入但无匹配: 显示"未能找到相关结果"
  - 有匹配结果: 完整展示所有匹配结果
  - 结果数超过5个: 提供滚动条功能

### 选择与插入
- **选择方式**: 
  - 键盘上下键导航
  - 鼠标点击选择
  - Tab键选择（避免触发输入框的发送操作）
- **插入行为**: 
  - 自动替换"/"及其后面的关键词
  - 插入选中提示词的完整内容
  - 完成后恢复初始状态

### 浮层显示持续策略
- **持续显示条件**: 只要有匹配结果，浮层就持续显示，不会自动消失
- **取消方式**: 用户按Esc键，或搜索词超过5个字符且无匹配时
- **无计时器关闭**: 不使用计时器自动关闭浮层，确保用户有足够时间选择

## 视觉样式
### 浮层设计
- **背景**: bg-magic-800，与页面背景区分
- **阴影**: shadow-xl，增强视觉层次感
- **内边距**: 适当内边距避免内容拥挤
- **位置**: 默认在光标下方；空间不足时调整到上方

### 联想结果展示
- **布局**: 卡片式布局清晰展示每个结果
- **内容**: 显示提示词标题和部分内容，悬停时显示完整内容
- **特殊标记**: 为收藏提示词添加星标或特殊标记
- **高亮**: 关键词匹配部分高亮显示

### 操作提示
- **底部操作栏**: 显示键盘快捷键说明
- **快捷键说明**: 
  - ↑/↓: 导航
  - Tab: 选择（移除Enter选择，避免触发发送）
  - Esc: 取消

## 相关性排序算法
### 计算公式
```
Score = (UsageWeight * NormalizedUsage) + (RecencyWeight * NormalizedRecency) + InitialScore
```

### 参数配置
- **使用频率权重(UsageWeight)**: 
  - 值: 0.7
  - 范围: 0.6 - 0.8
  - 说明: 使用次数是衡量提示词价值的重要指标
  - 调整策略: 用户倾向使用新提示词时可降低

- **时间权重(RecencyWeight)**: 
  - 值: 0.3
  - 范围: 0.2 - 0.4
  - 说明: 反映用户当前需求
  - 调整策略: 最近使用提示词过于集中时可降低

- **时间范围(TimeRange)**: 
  - 值: 14天
  - 范围: 7 - 30天
  - 说明: 根据提示词使用周期选择
  - 调整策略: 根据实际使用情况延长或缩短

- **冷启动常数(InitialScore)**: 
  - 值: 0.1
  - 说明: 解决新提示词初期得分过低问题
  - 计算依据: 应小于UsageWeight和RecencyWeight的最小值

### 排序优先级
1. **收藏状态**: 收藏的提示词优先显示
2. **相关性得分**: 在相同收藏状态下按得分排序
3. **归一化处理**: 使用归一化避免数值偏差

## 技术实现
### 组件生命周期
- **挂载**: 监听输入框内容变化
- **更新**: 根据用户输入更新联想状态
- **卸载**: 清理事件监听，恢复输入框状态

### 性能优化
- **防抖与节流**: 限制输入事件触发频率
- **增量搜索**: 避免不必要的全量渲染
- **虚拟滚动**: 处理大量联想结果时提高渲染性能

### 边缘情况处理
- **无匹配结果**: 显示提示信息，自动取消联想
- **输入框失焦**: 保存状态，允许恢复
- **多输入框支持**: 识别当前活动输入框，应用正确的联想上下文
- **删除搜索词处理**: 当用户删除搜索词时，重新执行搜索匹配逻辑

### 补充说明匹配机制

#### 2. **匹配机制**
• **触发条件**：输入框获得焦点且有输入“/”+文字的动作时
• **触发逻辑**（图示解析）：
  • 输入单字触发（如"健"→联想"健康管理"）
  • 输入连续子串触发（如"康管"→联想"健康管理"）
  • 输入非连续子串触发（如"康理"→联想"健康管理"）

#### 3. **匹配规则**
• **正向匹配**：输入字符出现在历史文本任意位置（首字优先）
• **逆向匹配**：支持倒序关键词（如输入"管理"→联想"健康管理"）
• **组合匹配**：输入分散字符形成逻辑关联（如"健管"→联想"健康管理"）
• *算法建议*：Trie树优化多模式匹配

#### 4. **排序规则**
• 高频使用条目置顶（如"健康管理"出现9次）
• 最近使用时间倒序
• 完全匹配优先（如输入"健康"时"健康管理"优先于"管理健康"）

#### 5. **交互规范**
• 下拉菜单最大显示10条联想项
• 通过↑↓键选择，Enter键确认
• 实时更新：继续输入时动态调整联想内容

---

### 三、特殊场景处理
1. **冲突处理**：
   • 输入"管理健康"时需同时联想"管理健康"和"健康管理"
   • 输入"康"时需匹配中间字（如"健康管理"）、结尾字（如"管理康"）

2. **去重规则**：
   • 完全相同的文本合并显示（如合并重复的"健康管理"）
   • 保留相似文本差异项（如"健康管理"与"健康管理管"）

---

### 四、技术指标
• **响应时间**：输入后200ms内返回联想结果
• **兼容性**：支持中/英/数字混合输入
---

### 五、验收用例（基于图示）
| 测试输入 | 预期联想结果 |
|---------|--------------|
| 健      | 健康管理（高频优先） |
| 康管    | 健康管理、管理健康管理 |
| 管理    | 健康管理、管理健康、管理康 |

通过以上结构化描述，既可清晰传达功能逻辑，又能为开发提供可验证的基准标准。建议配合流程图和UI原型图完善需求文档。