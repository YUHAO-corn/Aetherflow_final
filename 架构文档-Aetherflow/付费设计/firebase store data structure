# Firebase Firestore 数据结构 (根据截图推断)

## 顶级集合 (Root Collections)

### `users`

存储应用的用户信息。每个文档代表一个用户，文档 ID 通常是 Firebase Authentication 的 User ID。

```
/users/{userId}
```

*   **子集合 (Subcollections):**
    *   `membership`: 存储用户的会员信息。
    *   `prompts`: 存储用户创建的提示词。

#### `users/{userId}/membership`

存储特定用户的会员订阅状态。目前看来，似乎只有一个固定的文档 `status` 来存储状态信息。

```
/users/{userId}/membership/status
  ├── cancelAtPeriodEnd: boolean  // 是否在周期结束时取消订阅 (来自 Paddle)
  ├── customerId: string | null  // Paddle Customer ID
  ├── expiresAt: number | null   // 订阅到期时间戳 (毫秒)
  ├── lastVerifiedAt: number | null // 上次与 Paddle 核实状态的时间戳
  ├── plan: string | null      // 订阅的计划标识 (例如: "pro", "free", 或具体的 Paddle Plan ID)
  ├── startedAt: number | null   // 订阅开始时间戳
  ├── status: string           // 会员状态 ("pro", "free", "cancelled", etc.) - 应用内部状态
  ├── subscriptionId: string | null // Paddle Subscription ID
  ├── subscriptionStatus: string | null // Paddle 订阅状态 ("active", "past_due", etc.)
  └── updatedAt: number          // 此文档最后更新时间戳
```

*   **注意**:
    *   `status` 和 `subscriptionStatus` 可能有重叠，需要明确两者的区别和更新逻辑。`status` 看起来更像是应用内部维护的状态，而 `subscriptionStatus` 直接来自 Paddle。
    *   `plan` 字段在不同截图中显示为 `null` 或具体值 ("pro", "free")，需要确认其最终表示方式。

#### `users/{userId}/prompts`

存储用户创建的所有提示词。每个文档代表一个提示词。

```
/users/{userId}/prompts/{promptId}
  ├── id: string             // 提示词 ID (通常与文档 ID 相同)
  ├── content: string          // 提示词内容
  ├── createdAt: number        // 创建时间戳
  ├── isActive: boolean        // 是否为活动/选中状态 (UI 状态?)
  ├── isFavorite: boolean      // 是否收藏
  ├── isReadOnly: boolean      // 是否只读
  ├── lastUsed: number         // 最后使用时间戳
  ├── optimizationHistory: any[] | object | null // 优化历史记录 (结构不完全清晰)
  ├── productCategories: string[] | null // 产品类别标签? (结构不完全清晰)
  ├── source: string           // 来源 (例如: "user", "template")
  ├── tags: string[]           // 用户标签
  ├── title: string            // 提示词标题
  ├── updatedAt: number        // 最后更新时间戳
  ├── useCount: number         // 使用次数统计
  ├── userId: string           // 所属用户 ID (冗余，但方便查询)
  └── locked?: boolean        // (推测字段, 用于C阶段降级锁定) 是否因配额限制被锁定
```

*   **注意**:
    *   `optimizationHistory` 和 `productCategories` 的具体结构在截图中未完全展示。
    *   `locked` 字段是我们为阶段 C 规划的，需要确认是否已添加或计划添加。

### `customerMapping`

这个集合在截图中可见，但内部结构未知。**推测**其用途是将 Paddle 的 `customerId` 映射回 Firebase 的 `userId`，这在处理 Paddle Webhooks 时可能很有用，因为 Webhook 事件通常只带 `customerId`。

```
/customerMapping/{customerId}
  └── userId: string  // (推测) 对应的 Firebase User ID
```

---
