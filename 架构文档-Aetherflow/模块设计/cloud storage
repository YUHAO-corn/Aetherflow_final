# AetherFlow 云存储设计

## 核心设计原则

- **本地优先**: 所有操作先在本地执行，确保离线可用
- **透明同步**: 在后台自动同步数据，用户无需手动操作
- **渐进增强**: 基础功能无需登录，云功能作为增强特性
- **智能冲突处理**: 自动解决大多数冲突情况

## 数据存储架构

```
业务层(Hooks) → 存储抽象层(接口) → 具体实现(本地/云端)
```

### 关键组件

- **StorageService(接口)**: 统一存储API
- **ChromeStorageService**: 本地存储实现
- **CloudStorageService**: Firebase实现
- **SyncManager**: 同步协调器

## 同步策略设计选择：本地优先 vs 云端优先

### 本地优先策略概述

本地优先（Local-First）是一种将本地数据视为主要数据源的设计理念。在此策略下：

- 数据优先保存在本地设备上
- 所有核心功能在离线状态下正常工作
- 云端作为备份和同步媒介，而非主要数据源
- 同步被视为增强功能，不影响核心体验

### 云端优先策略概述

云端优先（Cloud-First）或最新优先（Latest-First）则将云端数据视为权威来源：

- 数据主要存储在云端服务器上
- 本地数据视为云端数据的缓存或副本
- 多数关键功能需要网络连接
- 实时性和数据一致性优先于离线可用性

### 不同产品的策略选择及原因

**实时协作工具（如Notion, Google Docs）采用云端优先策略**：
- 需要保证多用户同时编辑时的数据一致性
- 用户期望实时看到他人的编辑内容
- 离线功能相对次要，用户通常在有网络时使用
- 冲突解决策略简单直接，通常采用"最后写入胜出"

**个人笔记工具（如Evernote早期版本，传统OneNote）倾向于本地优先**：
- 个人使用场景下，离线可用性至关重要
- 同步可以延迟进行，不影响主要工作流
- 用户可能在飞机、地铁等无网环境下工作
- 冲突相对罕见，主要发生在跨设备同步

**混合型应用（如现代版OneNote，Apple Notes）采用双向策略**：
- 提供无缝在线体验，同时保证离线可用
- 在线时偏好云端数据，离线时完全依赖本地数据
- 复杂的冲突解决机制，通常创建冲突副本或合并视图
- 需要更复杂的同步逻辑和状态管理

### AetherFlow为何选择本地优先策略

AetherFlow选择本地优先策略主要基于以下考量：

1. **使用场景特性**：
   提示词管理是高度个人化的活动，很少需要实时协作。用户可能在各种环境下使用提示词，包括网络受限情况。

2. **数据安全性**：
   本地优先策略确保即使云服务不可用，用户仍然可以访问和使用所有提示词。这为用户提供了数据控制和安全保障。

3. **用户期望**：
   对于提示词工具，用户通常期望即开即用的体验，而不愿意等待云端同步完成才能开始工作。

4. **技术限制**：
   浏览器扩展环境对网络连接不稳定的情况需要有弹性设计，本地优先策略最适合这种环境。

## 同步策略实现

### 核心同步流程

1. **写入流程**
   - 数据先写入本地存储
   - 已登录状态下，添加到同步队列
   - 在线时直接上传到云端，离线时加入待处理队列

2. **读取流程**
   - 优先从本地存储读取
   - 已登录状态下，检查云端是否有更新版本
   - 发现云端更新则自动下载到本地

3. **主动同步流程**
   - 获取本地和云端所有提示词
   - 比较提示词版本，处理更新冲突
   - 本地独有提示词上传至云端
   - 云端独有提示词下载至本地

### 同步时机

- **即时同步**: 当有本地数据变更时立即触发
- **登录同步**: 用户登录成功后自动触发全量同步
- **联网同步**: 设备恢复网络连接后自动处理待同步项目
- **定期同步**: 在活跃使用期间每30分钟触发一次同步
- **手动同步**: 用户可通过UI手动触发同步

## 冲突解决策略

### 冲突类型与处理

本地优先策略下，冲突处理更为复杂，因为每个设备都可能有自己独立的修改历史。我们将冲突分为以下几类：

- **时间戳冲突**：同一ID不同更新时间，解决方法是保留最新版本
- **微小内容冲突**：相似度大于80%的修改，解决方法是保留最新版本
- **大幅内容冲突**：相似度小于80%的修改，解决方法是保留两个版本
- **删除冲突**：一端删除另一端更新，若本地更新较新则保留，否则删除

### 自动冲突解决流程

1. **检测是否为删除冲突**
   - 如本地/云端标记为已删除，应用删除保护策略

2. **评估内容差异程度**
   - 计算文本内容的相似度，确定冲突严重程度

3. **基于差异选择策略**
   - 轻微差异(<20%): 使用时间戳策略
   - 重大差异(>20%): 创建本地/云端副本，保留两版本

4. **冲突解决后处理**
   - 更新同步状态，记录已解决冲突
   - 需要时更新UI，提示用户冲突已解决

### 冲突解决示例

当用户在不同设备上编辑同一提示词时:

```typescript
// 检测到内容差异超过阈值
if (calculateDifference(localPrompt.content, cloudPrompt.content) > 0.2) {
  // 如果本地较新，保留本地并创建云端副本
  if (localPrompt.updatedAt >= cloudPrompt.updatedAt) {
    const cloudCopy = {...cloudPrompt};
    cloudCopy.id = generateUniqueId();
    cloudCopy.title = `${cloudCopy.title} (云端版本)`;
    
    // 保存副本
    await chromeStorageService.savePrompt(cloudCopy);
    
    return localPrompt; // 以本地版本为主
  } else {
    // 否则保留云端并创建本地副本
    const localCopy = {...localPrompt};
    localCopy.id = generateUniqueId();
    localCopy.title = `${localCopy.title} (本地版本)`;
    
    // 保存副本
    await chromeStorageService.savePrompt(localCopy);
    
    return cloudPrompt; // 以云端版本为主
  }
}
```

## 同步状态显示

### UI设计

- **同步状态指示器**: 位于应用右下角的小型图标
- **状态表示**:
  - **已同步(绿色)**: 数据已完全同步
  - **同步中(蓝色旋转)**: 数据正在同步
  - **离线(灰色)**: 无法连接服务器
  - **错误(红色)**: 同步过程中出错
- **交互功能**: 点击图标可手动触发同步，长按可显示详细信息

### 实现方式

```tsx
const SyncStatusIndicator = () => {
  const { syncStatus, isSyncing, syncError, syncAll } = useCloudSync();
  
  return (
    <div className="sync-indicator" onClick={syncAll} title={getStatusText()}>
      {renderStatusIcon()}
    </div>
  );
};
```

## 行业标准对比与策略演进

### 主流产品同步策略演进

**Notion**采用了极致的云端优先策略，几乎所有操作都依赖服务器。它优先考虑协作体验和实时性，冲突处理采用"最后写入胜出"的简单模式。缺点是离线能力有限，网络连接不稳定时用户体验明显下降。

**Evernote**经历了从本地优先到云端偏好的演变。早期版本完全基于本地数据库，同步是次要功能；而现代版本更依赖云服务，但仍保留较强的离线支持。它的冲突处理倾向于创建冲突副本，保证数据不丢失但可能导致重复笔记。

**OneNote**采用了一种混合方法，在线时更像云端优先应用，离线时表现为本地优先系统。它的冲突解决更加细粒度，能够在段落级别标记和解决冲突。这种方法提供了较好的灵活性，但也增加了实现复杂度。

**Apple Notes**初期是纯本地应用，随着iCloud的发展逐渐演变为混合模式。它保持了强大的离线功能，同时在有网络时提供近乎无缝的同步体验。Apple的优势在于控制了整个生态系统，可以在系统级别优化同步体验。

### AetherFlow的差异化方案

相比这些产品，AetherFlow的云同步设计有以下特点：

1. **更重视离线体验**：所有功能离线可用，云同步作为增强特性而非必需功能。这确保了用户在任何网络条件下都能高效使用工具。

2. **更智能的冲突处理**：根据内容差异程度采用不同策略，避免简单的"最后写入胜出"可能导致的数据丢失问题。这种智能差异处理在保障数据安全的同时，减少了用户处理冲突的负担。

3. **更低网络消耗**：采用增量同步和智能队列机制，仅同步必要的数据变更，减少带宽使用和电池消耗。

4. **更高透明度**：同步状态清晰可见但不打断用户工作流，让用户随时了解同步状态而不受干扰。

## 实现注意事项

- 使用Firebase的Firestore作为云存储后端
- 采用懒加载策略，避免一次性加载大量数据
- 实现断点续传，解决网络不稳定情况
- 添加重试机制，处理临时连接问题

## 结论

AetherFlow的云存储系统基于"本地优先"理念，确保用户在任何网络状态下都能正常使用应用核心功能。云同步作为增强特性，通过智能的同步策略和冲突处理机制，为用户提供近乎无缝的多设备体验。

通过对比不同产品的同步策略，我们可以看到没有放之四海而皆准的最佳方案——正确的选择取决于应用的具体需求、用户场景和优先级。对于AetherFlow这样的个人提示词管理工具，本地优先策略是最合理的选择，它在保证核心功能稳定可靠的基础上，提供了云端同步的便利性。
