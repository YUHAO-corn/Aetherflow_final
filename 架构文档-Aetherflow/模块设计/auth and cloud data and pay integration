# AetherFlow 认证功能设计

## 0. 开发顺序与实施策略

为确保功能开发的顺利进行和系统架构的稳定性，以下是推荐的开发顺序：

### 0.1 阶段一：用户认证功能（已实现）

**原因**：
- 认证是整个系统的基础，其他功能都依赖于用户身份
- 相对独立且配置简单，可以快速实现并验证
- 为后续功能提供用户上下文和权限控制基础
- 尽早完成认证UI有助于建立整体用户体验基调

**实施内容**：
1. ✅ 配置Firebase Authentication
2. ✅ 实现基础邮箱/密码注册登录
3. ✅ 开发认证UI组件（登录按钮、头像显示）
4. ✅ 实现认证状态管理（持久化、多标签页同步）
5. ✅ 添加第三方登录（Google登录）
6. ✅ 完善账户管理功能

### 0.2 阶段二：云端存储功能（已实现）

**原因**：
- 依赖于已完成的认证系统
- 属于核心价值功能（跨设备同步）
- 需要充分测试各种同步场景和边界条件
- 存储层改动会影响全局应用状态

**实施内容**：
1. ✅ 设计Firestore数据库结构
2. ✅ 实现基本数据同步功能
3. ✅ 开发本地与云端同步机制
4. ✅ 添加离线支持与操作日志
5. ✅ 设计并实现冲突检测与解决
6. ✅ 优化同步性能和数据安全

### 0.3 阶段三：付费功能（待实现）

**原因**：
- 依赖于认证和云存储功能的完整实现
- 涉及实际金钱交易，需要在其他核心功能稳定后开发
- 配额限制需要基于已有功能（存储量、优化次数）
- 集成支付系统通常需要额外审核和测试时间

**实施步骤**：
1. 实现配额限制逻辑
2. 开发付费UI和升级引导流程
3. 集成Paddle支付系统
4. 实现会员状态管理和权益应用
5. 完善降级处理和异常场景支持

### 0.4 跨阶段任务

**持续进行的工作**：
- ✅ 同步开发对应的单元测试和集成测试
- ✅ 构建Firebase服务抽象层，避免直接依赖
- ✅ 定期安全审查，确保数据和认证安全
- ✅ 用户反馈收集和功能迭代优化

## 1. Firebase云端存储功能

### 1.1 功能概述
已实现基于Firebase的云端存储系统，支持用户提示词的跨设备同步和备份。该系统遵循"本地优先"策略，确保在无网络环境下仍可使用核心功能。

### 1.2 用户价值
- 用户可以在多台设备上访问相同的提示词库
- 提示词数据不会因浏览器数据清除而丢失
- 支持更大容量的提示词存储

### 1.3 已实现功能与验收标准

#### 数据同步
- **已实现**：提供透明的数据同步机制，用户在一台设备上的更改会反映到其他已登录设备
- **同步性能**：使用增量同步和批处理机制，优化同步性能
- **状态显示**：通过SyncStatusIndicator组件显示同步状态（已同步、同步中、离线、错误）

#### 离线支持
- **已实现**：完整的离线支持功能，断网状态下可使用所有已同步的提示词
- **操作队列**：通过pendingOperations机制存储离线操作，恢复网络后自动同步
- **状态指示**：清晰显示当前离线/在线状态，以及同步状态

#### 冲突处理
- **已实现**：智能冲突解决机制，根据内容差异程度采用不同策略
- **差异处理**：轻微差异采用最新版本策略，重大差异保留两个版本
- **删除保护**：防止意外覆盖重要内容的删除保护机制

#### 数据安全
- **已实现**：使用Firebase安全规则限制数据访问，确保用户隐私
- **认证集成**：所有云存储操作需要有效的认证令牌
- **传输加密**：所有网络传输使用HTTPS加密

### 1.4 实际实现细节

#### 存储架构
- **统一接口**：所有存储操作通过`StorageService`接口执行，确保一致性
- **本地存储**：使用`chromeStorageService`实现本地存储
- **云存储**：使用`cloudStorageService`实现云端同步
- **工厂模式**：通过`getStorageService()`工厂函数选择合适的实现

#### 同步策略
- **本地优先**：所有操作先在本地执行，确保离线可用
- **透明同步**：在后台自动同步数据，用户无需手动操作
- **增量机制**：只同步有更改的数据，降低网络开销
- **冲突解决**：使用内容差异比较和时间戳策略解决冲突

#### 同步触发时机
- **即时同步**：当有本地数据变更时立即触发
- **登录同步**：用户登录成功后自动触发全量同步
- **联网同步**：设备恢复网络连接后自动处理待同步项目
- **手动同步**：用户可通过UI手动触发同步

## 2. 用户认证功能

### 2.1 功能概述
已实现基于Firebase Authentication的用户认证系统，支持注册、登录、密码重置和第三方认证。

### 2.2 用户价值
- 用户可以创建个人账户保存数据
- 支持跨设备访问个人提示词库
- 可使用Google账号快速登录

### 2.3 已实现功能与验收标准

#### 认证方式
- **已实现**：邮箱/密码认证，支持注册、登录
- **已实现**：Google账号登录
- **流程优化**：简化认证流程，减少用户操作步骤

#### 登录体验
- **已实现**：登录状态在所有打开的窗口同步
- **已实现**：登录状态在Chrome存储中保持，避免频繁登录
- **UI优化**：登录/注册表单提供实时验证反馈

#### 账户管理
- **已实现**：用户可更改密码
- **已实现**：提供"忘记密码"功能
- **已实现**：用户可管理自己的账户信息

#### 认证状态管理
- **已实现**：认证状态在应用各部分保持一致
- **已实现**：令牌过期时自动引导重新认证
- **已实现**：多标签页间认证状态同步

### 2.4 实际实现细节

#### 认证服务设计
- **统一接口**：通过`AuthService`接口定义认证操作
- **Firebase集成**：使用Firebase Auth实现认证功能
- **状态持久化**：认证状态存储在Chrome存储中，支持跨会话持久化
- **订阅模式**：通过`onAuthStateChanged`方法提供观察者模式

#### 用户状态管理
- **类型定义**：完整的`User`类型定义，包含用户资料和认证信息
- **状态同步**：用户登录/登出状态自动同步到Chrome存储
- **钩子函数**：提供`useAuth`钩子简化认证状态管理

#### 安全考量
- **令牌管理**：安全存储认证令牌，仅在需要时使用
- **错误处理**：认证错误提供友好反馈，引导用户解决问题
- **隐私保护**：符合Chrome扩展政策的隐私实践

## 3. 付费功能与会员等级

### 3.1 功能概述（待实现）
基于已完成的认证和云存储系统，计划实现简化的付费模式，区分免费用户和付费会员，无额外会员专属功能。

### 3.2 用户价值
- 免费用户可体验核心功能，了解产品价值
- 付费会员获得更高使用配额，提升使用体验
- 简单明了的付费模式，无隐藏费用

### 3.3 成功标准与验收测试

#### 配额限制实现
- **标准**：系统准确跟踪并执行配额限制，不出现误判
- **验收测试**：
  - 免费用户存储第6个提示词时，系统准确阻止并提示升级
  - 免费用户进行第4次优化时，系统准确阻止并提示升级
  - 付费会员可存储至少100个提示词且每日可进行50次优化
  - 配额限制准确执行，无需常态显示使用量

#### 升级流程
- **标准**：升级过程简单直观，支付流程安全可靠
- **验收测试**：
  - 从点击"升级"到完成支付不超过3个步骤：选择方案 → 支付 → 确认
  - 支付使用Paddle实现，流程符合Chrome扩展商店规范
  - 支付成功后，会员权益在5秒内生效
  - 提供明确的支付成功反馈

#### 订阅管理
- **标准**：用户可轻松管理订阅状态，获取清晰的账单信息
- **验收测试**：
  - 账户页面清晰显示当前订阅计划和续费日期
  - 用户可通过Paddle客户门户管理订阅
  - Paddle提供订阅历史和收据下载
  - 订阅状态变更后，发送确认电子邮件

#### 付费UI与体验
- **标准**：付费相关UI友好且非侵入式，不干扰核心用户体验
- **验收测试**：
  - 配额用尽时才显示升级提示，不对免费用户展示频繁推销
  - 升级按钮位于认证功能附近，使用火箭图标或"PRO"标签，配色与品牌一致
  - 明确传达免费版与付费版的区别，无夸张宣传
  - 达到免费限制的80%时，提供一次温和提醒

#### 付费UI组件
- **标准**：提供清晰明确的付费相关UI组件
- **验收测试**：
  - 会员标识：付费用户在个人头像旁显示PRO标签或小型火箭图标，渐变蓝紫色
  - 升级卡片：在配额用尽时显示的优雅卡片，包含主要配额对比和明确的价格
  - 会员特权提示：在限制页面展示"会员无限制"等文案
  - 支付完成动画：支付成功后显示简洁愉悦的成功动画

#### 优化次数配额管理
- **标准**：优化次数限制准确执行，不打断用户体验
- **验收测试**：
  - 免费用户次数用尽时，优化按钮变为"升级解锁更多次数"
  - 付费用户次数用尽时显示"今日配额已用完，明天再来"提示
  - 配额在UTC 00:00准时重置
  - 不需要常态显示剩余次数，保持界面简洁

#### 降级处理
- **标准**：订阅到期后，系统平滑降级用户体验，不造成数据丢失
- **验收测试**：
  - 订阅到期后，超出免费限制的数据仍保留但标记为"只读"
  - 只读状态下，卡片变暗，中央显示锁图标，不可复制、不可调用、不可编辑
  - 点击锁图标或尝试与只读卡片交互时，弹出升级提示
  - 重新订阅后，所有功能和数据立即恢复

### 3.4 技术实现建议

#### 配额检查实现
需要在现有存储服务的基础上添加配额检查机制：
```typescript
// 建议在存储服务层添加配额检查
async savePrompt(prompt: Prompt): Promise<void> {
  // 获取当前用户
  const currentUser = await authService.getCurrentUser();
  const isPremium = currentUser?.isPremium || false;
  
  // 检查存储配额
  const allPrompts = await this.getAllPrompts();
  const activePrompts = allPrompts.filter(p => p.isActive !== false);
  
  // 应用配额限制
  if (!isPremium && activePrompts.length >= STORAGE_LIMITS.FREE_MAX_PROMPTS) {
    throw new QuotaExceededError('存储空间已满，请升级到专业版获取更多空间');
  }
  
  // 正常处理保存
  // ...现有存储逻辑...
}
```

#### 付费状态检查
建议在`User`接口添加会员标识：
```typescript
export interface User {
  // ...现有字段...
  isPremium: boolean;
  membershipExpiry?: number; // 会员过期时间戳
  membershipTier?: 'free' | 'basic' | 'pro'; // 会员等级
}
```

#### 降级处理建议
需要考虑会员过期后的数据处理：
```typescript
// 查询提示词时添加访问控制
async getPrompt(id: string): Promise<Prompt | null> {
  const prompt = await chromeStorageService.getPrompt(id);
  
  // 如果存在且用户未过期，或是免费额度内，则允许完全访问
  const currentUser = await authService.getCurrentUser();
  const isPremium = currentUser?.isPremium || false;
  
  if (!prompt) return null;
  
  // 检查是否超出免费用户配额但非会员
  if (!isPremium && id > STORAGE_LIMITS.FREE_MAX_PROMPTS) {
    // 返回只读版本
    return {
      ...prompt,
      isReadOnly: true
    };
  }
  
  return prompt;
}
```

### 3.5 与现有实现的集成计划

根据现有认证和云存储实现，建议按以下步骤集成付费功能：

1. **用户模型扩展**
   - 在`User`类型中添加会员状态属性
   - 在Firebase用户记录中存储会员信息

2. **Paddle集成**
   - 注册Paddle账户并设置产品
   - 配置Webhook接收端点
   - 添加Paddle.js到应用

3. **服务层扩展**
   - 添加`SubscriptionService`接口定义付费相关操作
   - 实现配额检查和权限控制逻辑
   - 设计Webhook处理逻辑

4. **UI组件实现**
   - 添加会员标识显示
   - 设计升级提示和引导流程
   - 实现配额用尽提示界面

5. **数据层处理**
   - 添加只读模式标记
   - 实现会员等级权限检查
   - 提供配额使用情况统计

## 4. 边界条件与异常场景

### 4.1 网络不稳定场景
- **已实现**：检测网络状态变化，自动切换在线/离线模式
- **已实现**：使用本地存储和待处理操作队列保证数据一致性
- **已实现**：网络恢复后智能处理冲突和待同步操作

### 4.2 设备存储受限场景
- **已实现**：设置存储限制常量，防止过度使用设备存储
- **建议**：添加存储空间检查，在接近限制时提供清理建议
- **建议**：实现数据压缩和优先级存储策略

### 4.3 账户安全异常场景
- **已实现**：基于Firebase Auth的安全认证机制
- **建议**：添加异常登录检测和通知
- **建议**：提供账户活动日志功能

### 4.4 API限制和服务中断场景
- **已实现**：错误处理和重试机制
- **已实现**：降级到本地功能的机制
- **建议**：添加更细粒度的错误分类和处理策略

### 4.5 支付处理异常场景（待实现）
- **建议**：详细的Paddle支付错误处理和用户反馈
- **建议**：订阅状态定期验证和续费提醒
- **建议**：使用Paddle提供的客户门户进行订阅管理

## 5. 跨功能需求

### 5.1 性能标准
- **已实现**：优化Firebase操作，减少网络请求
- **已实现**：后台同步不阻塞UI线程
- **已实现**：延迟加载和按需同步策略

### 5.2 安全标准
- **已实现**：认证令牌安全存储和处理
- **已实现**：数据加密传输和存储
- **已实现**：权限控制和访问限制

### 5.3 可访问性标准
- **已实现**：状态变化有视觉和文本指示
- **已实现**：错误信息清晰可理解
- **建议**：进一步增强键盘导航和辅助技术支持

### 5.4 国际化标准
- **建议**：添加多语言支持框架
- **建议**：日期、时间和货币格式本地化
- **建议**：支持多语言错误信息和提示

## 6. UI视觉设计方案

### 6.1 总体设计风格

遵循AetherFlow现有"魔法流体"设计语言，保持暗色主题、渐变色彩和流动感动效。

#### 色彩规范
- **主色调**：深蓝/紫色调 (#1e293b, #0f172a)
- **强调色**：魔法紫 (#7c3aed, #8b5cf6)
- **会员标识色**：渐变蓝紫色（#8b5cf6 → #7c3aed）

### 6.2 认证功能UI

#### 登录/注册入口（已实现）
- **位置**：侧边栏右上角
- **视觉**：未登录状态显示登录按钮，登录后显示用户头像
- **交互**：点击头像显示下拉菜单，提供账户管理选项

#### 认证模态框（已实现）
- **内容**：提供表单输入区域和社交登录选项
- **设计**：使用一致的深色主题和高对比度界面
- **交互**：表单验证实时反馈，操作状态明确提示

#### 用户头像显示（已实现）
- **样式**：圆形头像或字母头像（若无头像）
- **位置**：保持在侧边栏右上角固定位置
- **状态**：不同状态（登录、加载中）有清晰视觉区分

### 6.3 云端存储UI

#### 同步状态指示器（已实现）
- **位置**：侧边栏底部区域
- **视觉**：使用颜色编码表示不同同步状态
  - 已同步：绿色
  - 同步中：蓝紫色，带旋转动画
  - 离线：灰色
  - 错误：红色
- **交互**：点击可触发手动同步，悬停显示详细状态信息

### 6.4 付费功能UI（待实现）

#### 会员徽章
- **位置**：用户头像右上角
- **尺寸**：14px × 14px圆形
- **视觉**：
  - 渐变蓝紫色背景 (#8b5cf6, #7c3aed)
  - 显示PRO标签或小型火箭图标
  - 发光效果动画
- **交互**：悬停显示"专业会员"提示

#### 配额用尽卡片
- **尺寸**：与现有Card组件一致
- **视觉**：
  - 标题区：清晰表明限制类型
  - 中部：对比性展示免费vs会员配额
  - 右上角：PRO标识或火箭图标
  - 底部：升级按钮，使用紫色渐变背景
- **交互**：卡片和按钮添加HoverEffect，配合Shimmer效果

#### 升级会员弹窗
- **尺寸**：400px宽
- **视觉**：
  - 价格标签：突出显示，使用品牌色
  - 特权列表：每项前配图标，使用圆形紫色背景
  - 背景：添加淡化的MagicParticles效果
  - 行动按钮：渐变紫色背景，宽大可点击区域
- **交互**：入场动画，按钮点击效果，粒子背景微动

#### 只读提示词卡片
- **视觉**：
  - 半透明黑色遮罩 (rgba(0, 0, 0, 0.5))
  - 中央锁图标 (紫色，#8b5cf6)
  - 淡紫色背景发光效果
- **交互**：点击显示升级提示，悬停锁图标放大

### 6.5 响应式适配

- **已实现**：侧边栏UI适应不同屏幕尺寸
- **已实现**：组件布局根据可用空间自动调整
- **建议**：为非常小的屏幕提供更紧凑的备选布局
