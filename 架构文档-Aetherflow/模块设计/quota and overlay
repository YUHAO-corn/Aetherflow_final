# 配额管理与限制体验 (Quota & Overlay) - PRD 草稿

## 1. 关键信息与目标 (Key Information & Goals)

-   **目标**: 实现配额管理和限制体验，清晰展示免费/Pro会员权益差异，引导升级。
-   **核心用户体验**: 
    -   非侵入式提醒，直到达到限制。
    -   清晰告知用量、限制及操作受阻原因。
    -   通过限制反衬 Pro 价值。
    -   平滑的(降级)状态过渡，数据不丢失 (锁定代替删除)。
-   **涉及配额**: 
    -   提示词存储数量 (Storage)
    -   优化使用次数 (Optimization) - 每日重置
-   **关联阶段**: 付费功能阶段 C

## 2. 技术规划概要 (Technical Plan Summary)

-   **核心服务 (`QuotaService`)**: 
    -   位置: `extension/src/services/quota/`
    -   职责: 定义、存储、计算、查询配额用量与限制，与 `MembershipService` 联动。
    -   数据存储: 倾向于单独 Firestore 文档 `users/{userId}/quota/status` (待确认)。
-   **后台任务 (Background Script)**:
    -   使用 `chrome.alarms` 实现优化次数每日重置。
-   **集成点**: 
    -   在 `PromptService` (或相关 Hook) 中检查存储配额。
    -   在优化功能的服务/Hook 中检查和增加优化次数配额。
-   **降级处理**: 
    -   由 `MembershipService` 触发，`PromptService` 执行锁定逻辑 (更新 `prompt.locked` 字段)。
-   **UI 组件**:
    -   `QuotaBanner`: (位置 `components/common/` 或 `components/quota/`) 在达到限制并尝试操作时显示。
    -   `LockedPromptCard`: (位置 `components/library/`) 展示被锁定的提示词。
-   **错误处理**: 
    -   定义 `QuotaExceededError`，由服务层抛出，UI 层捕获并展示相应反馈 (如横幅)。

## 3. 详细需求与 UI/UX (Detailed Requirements & UI/UX)

### 3.1 配额定义 (Quota Definition - Task C1 Refined)

-   **存储配额 (Storage Quota)**:
    -   **免费用户**: 最多存储 **5** 条提示词。
    -   **Pro 会员**: 最多存储 **100** 条提示词。
    -   **计算方式**: 计算 `users/{userId}/prompts` 集合下的文档数量。
-   **优化次数配额 (Optimization Quota)**:
    -   **免费用户**: 每日 **3** 次优化机会。
    -   **Pro 会员**: 每日 **50** 次优化机会。
    -   **计算方式**: 记录当日优化功能成功执行的次数。
    -   **重置**: 每日 **凌晨 00:00 (用户本地时间)** 自动重置免费用户的优化次数为 3，Pro 会员重置为 50。

### 3.2 配额检查与行为 (Quota Check & Behavior - Task C5 Refined)

-   **场景 1: 用户尝试创建新提示词**
    -   **检查**: 调用 `quotaService.canUseFeature('storage')`。
    -   **免费用户达到上限 (5条)**: 
        -   操作**阻止**: 不允许创建新提示词。
        -   **UI 反馈**: 首次触发时，在提示词列表顶部显示**存储配额限制横幅 (QuotaBanner)** (见 3.4)。
        -   **后续触发**: 显示一个简短的 Toast 提示 "提示词存储已达上限，升级 Pro 解锁更多存储。" (避免重复显示横幅)。
    -   **Pro 会员达到上限 (100条)**: 
        -   操作**阻止**: 不允许创建新提示词。
        -   **UI 反馈**: 显示 Toast 提示 "提示词存储已达上限(100/100)。请删除部分不常用的提示词以继续创建。"
    -   **未达上限**: 允许创建。
-   **场景 2: 用户尝试执行优化功能**
    -   **检查**: 调用 `quotaService.canUseFeature('optimization')`。
    -   **免费用户达到上限 (3次)**:
        -   操作**阻止**: 不允许执行优化。
        -   **UI 反馈**: 首次触发时，在相关操作区域（如优化按钮旁或编辑器顶部）显示**优化配额限制横幅 (QuotaBanner)** (见 3.4)。
        -   **后续触发**: 显示 Toast 提示 "今日优化次数已用尽，升级 Pro 获得更多优化次数。" (避免重复显示横幅)。
    -   **Pro 会员达到上限 (50次)**: 
        -   操作**阻止**: 不允许执行优化。
        -   **UI 反馈**: 显示 Toast 提示 "今日优化次数已用尽(50/50)。次数将在明天凌晨自动刷新。"
    -   **次数增加**: 只有在优化**成功完成**后，才调用 `quotaService.incrementUsage('optimization')`。
    -   **未达上限**: 允许优化。

### 3.3 降级处理 (Downgrade Handling - Task C2 Refined)

-   **触发**: `MembershipService` 检测到用户状态从 `pro` 变为 `free`。
-   **逻辑**: 
    1. 获取免费用户存储配额 (5条)。
    2. 统计用户当前拥有的提示词数量。
    3. 如果数量 > 5：
        -   按照**创建时间 (`createdAt`)** 降序排序，保留最新的 5 条。
        -   将其余 (旧的) 提示词文档的 `locked` 字段更新为 `true`。
-   **用户感知**: 用户不会收到直接的通知说"你的 XX 提示词被锁定了"，而是会在浏览提示词库时看到被锁定的卡片 (见 3.5)。
-   **解锁**: 当用户重新升级为 `pro` 时，`MembershipService` 触发解锁流程，将该用户所有提示词的 `locked` 字段更新为 `false` 或移除。

### 3.4 配额限制横幅 (Quota Banner - Task C3)

-   **组件**: `QuotaBanner`
-   **触发时机**: 用户达到某项配额限制，并**尝试执行**相关超额操作时首次显示。
-   **显示位置**: 
    -   存储限制: 提示词列表 (Library) 顶部。
    -   优化限制: 优化功能区域（如编辑器）顶部或相关按钮附近。
-   **内容 (示例)**:
    -   **存储限制**: "您的提示词库已满 (5/5)。升级到 Pro 版本可存储更多提示词，并解锁文件导出等高级功能！[升级到 Pro] [了解更多] [关闭]" 
    -   **优化限制**: "今日优化次数已用尽 (3/3)。升级到 Pro 版本可获得每日 50 次优化，并解锁文件导出等高级功能！[升级到 Pro] [了解更多] [关闭]"
-   **交互**: 
    -   `[升级到 Pro]`: 点击跳转到官网支付页面 (带来源参数 `?from=quota_banner_storage` 或 `quota_banner_optimization`)。
    -   `[了解更多]`: 跳转到官网。可复用 **UpgradeButton** 组件的功能，包括点击跳转官网、hover 出现升级卡片、跳转官网带有认证 token 等功能。
    -   `[关闭]`: 点击隐藏横幅 (本次会话内不再因同类限制自动弹出，但下次启动或手动刷新后可能再次出现)。
-   **UI 设计**: 
    -   样式：横幅应采用浅黄色背景 (#FFF8E1)，与应用整体配色方案保持协调。
    -   图标：使用警告/提示图标 (⚠️ 或 ℹ️)，位于横幅左侧。
    -   字体：正文使用 14px，与应用其他文本保持一致。
    -   按钮样式：
        -   "升级到 Pro"：突出按钮，使用品牌主色 (#4A6CF7) 填充，白色文字，圆角 4px。
        -   "了解更多"：次要按钮，仅文字带下划线，使用品牌主色。
        -   "关闭"：灰色文字按钮或 X 图标。
    -   布局：文字提示在左侧占据大部分空间，按钮在右侧对齐排列。
    -   动画：横幅平滑滑入/滑出效果，300ms 过渡时间。
    -   响应式：在窄屏环境下，按钮可垂直排列，确保文本和按钮都完全可见。

### 3.5 锁定提示词卡片 (Locked Prompt Card - Task C4)

-   **组件**: 修改现有 `PromptCard` 组件，增加 `locked` 状态。
-   **触发时机**: 当渲染的提示词数据 `locked` 字段为 `true` 时。
-   **视觉表现**: 
    -   卡片整体覆盖一层半透明灰色遮罩 (rgba(0,0,0,0.5))。
    -   卡片中央显示一个明显的"锁定"图标 (使用锁图标 🔒，大小 24px)。
    -   卡片原内容保持可见但降低透明度 (opacity: 0.6)。
    -   鼠标悬停 (Hover) 时:
        -   遮罩变暗 (rgba(0,0,0,0.7))。
        -   显示文字提示，例如: "此提示词已锁定。升级到 Pro 解锁所有提示词，并获得每天 50 次优化！"
        -   显示一个醒目的 "[升级解锁]" 按钮（与 QuotaBanner 中的升级按钮保持一致的视觉风格）。
-   **交互**: 
    -   点击卡片本身不执行默认操作（如加载到编辑器）。
    -   点击 "[升级解锁]" 按钮: 跳转到官网支付页面 (带来源参数 `?from=locked_prompt_card`)。
-   **UI 设计**:
    -   锁定状态文本：白色 (#FFFFFF)，阴影确保在任何背景上可读。
    -   升级按钮：品牌主色 (#4A6CF7)，白色文字，圆角 4px，鼠标经过时略微放大。
    -   提示信息位置：卡片中央偏下，最大宽度为卡片宽度的 80%，确保文本不溢出。
    -   过渡动画：hover 状态平滑过渡，200ms 过渡时间。
-   **注意**: 锁定的提示词**不应**被计入免费用户的当前存储用量（因为它们不可用），但它们仍然占用用户的总存储空间。

### 3.6 Toast 提示设计 (Toast Notification - Task C5)

-   **使用场景**:
    -   **首次尝试超额操作后的再次尝试**: 当用户首次触发限制并看到横幅后，如果关闭横幅再次尝试同样的操作，使用 Toast 提示而非再次显示横幅。
    -   **Pro 会员达到限制**: Pro 会员达到存储或优化次数限制时，使用 Toast 而非横幅通知。
    -   **快速操作反馈**: 当用户在短时间内多次尝试超额操作时。
-   **显示规则**:
    -   **时长**: Toast 显示 3 秒后自动消失。
    -   **位置**: 页面顶部中央或右上角，与应用其他 Toast 保持一致位置。
    -   **堆叠**: 当多个 Toast 同时触发时，新的 Toast 应替换旧的 Toast，避免堆积。
    -   **频率限制**: 相同内容的 Toast 在 5 秒内不重复显示，避免反复弹出干扰用户。
-   **UI 设计**:
    -   **样式**: 深色背景 (#333333) 半透明 (85% 不透明度)，白色文字，4px 圆角。
    -   **图标**: 根据类型使用不同图标：警告/限制使用 ⚠️，信息性提示使用 ℹ️。
    -   **大小**: 宽度自适应文本长度，但最大不超过屏幕宽度的 80%。
    -   **动画**: 淡入淡出效果，300ms 过渡时间。
    -   **互动**: 用户可点击 Toast 提前关闭。
    -   **附加按钮**: 对于存储/优化限制，可包含一个小型"升级"链接。

## 4. 边界问题与解决方案 (Edge Cases & Solutions)

-   **配额计数精确性**: 离线不会影响用量计数，快速连续操作的影响很小，无需特别处理。

-   **每日重置方案评估**:
    -   **方案A: 不同时区 0 点每日刷新**
        -   **优点**: 符合用户对"每日"的自然理解，与日历日相匹配。
        -   **缺点**: 不同时区用户体验不一致；实现复杂度较高，需要正确处理时区；若用户切换时区可能导致计数错误。
        -   **技术实现**: 使用 `chrome.alarms` API，根据用户本地时区设置每日重置时间。
    -   **方案B: 每 X 小时恢复 1 次使用次数**
        -   **优点**: 不依赖时区，所有用户体验一致；技术实现简单；提供更频繁的使用机会。
        -   **缺点**: 可能导致用户困惑，"每日"概念变得模糊；不符合常见产品实践；计数方式不直观。
        -   **技术实现**: 基于固定间隔 (例如每 8 小时恢复 1 次)，记录上次恢复时间和当前可用次数。
    -   **推荐方案**: 采用**方案 A**，尊重"每日"的自然含义，并通过 UI 提示清晰传达剩余次数和下次重置时间。技术实现方面，使用 `chrome.alarms` 设置本地时区午夜触发的定时任务进行重置。

-   **匿名用户初始化**: 
    -   当检测到用户为 Firebase 匿名用户时，`QuotaService` 应正确初始化用户的配额状态，但需要确保不会重复初始化。
    -   在 `QuotaService` 初始化流程中，检查用户类型。若为匿名用户，正确设置默认配额状态。
    -   如果匿名用户后续注册/登录，确保配额数据正确合并，不会丢失已使用的次数。

-   **降级瞬间的操作**: 如果用户在降级发生的瞬间正在进行操作（例如，保存第六条提示词），该操作可能成功，但刷新后用户会看到部分提示词被锁定，这是可接受的边界情况。

-   **数据同步策略**: UI 优先显示本地状态，后台异步与 Firestore 同步。这样可以保证操作的即时反馈，同时确保数据最终一致性。

-   **UI 适配与一致性**:
    -   所有新增 UI 组件（横幅、锁定卡片状态、Toast）需与现有应用风格保持一致。
    -   使用应用现有的色彩方案、字体和间距规范。
    -   响应式设计确保在不同尺寸屏幕上正常展示。
    -   与现有组件库的交互模式保持一致，避免引入新的交互范式。
