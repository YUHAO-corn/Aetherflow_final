---
title: AetherFlow - 配额管理与限制体验产品需求文档 (PRD)
version: 1.0
date: 2024-07-27
status: Draft
---

## 1. 引言

本文档旨在详细定义 AetherFlow 应用程序中配额管理、限制体验及会员降级处理的功能需求和用户界面设计。此模块的核心目标是有效区分免费版和 Pro 专业版的用户权益，在用户达到免费资源使用上限时提供清晰、友好的引导，并在用户从 Pro 版降级时妥善处理超额数据，最终鼓励用户升级到 Pro 版本。

本文档将作为 AI 编程的主要依据，力求清晰、准确、完整。

## 2. 目标

*   **建立清晰的权益区分**: 通过配额限制，明确界定免费版和 Pro 版在核心资源（如提示词存储数量、每日优化次数）上的差异。
*   **提供无缝的限制体验**: 当免费用户达到配额上限时，以非侵入性、信息明确的方式告知用户，并提供便捷的升级路径。
*   **实现公平的降级处理**: 当 Pro 用户降级为免费用户时，对其超出免费配额的数据进行锁定处理，而非直接删除，保护用户数据并引导其重新升级。
*   **驱动用户升级**: 通过展示 Pro 版的价值（解除限制、更多资源），激励免费用户升级。
*   **确保系统健壮性**: 配额计算、检查、重置和状态联动机制需准确可靠。

## 3. 核心概念

*   **配额 (Quota)**: 用户在特定时间段内（如永久、每日）可使用的特定资源的数量上限。
*   **限制 (Limits)**: 为不同会员等级（免费版、Pro 版）定义的具体配额数值。
*   **用量 (Usage)**: 用户当前已使用的资源数量。
*   **配额检查 (Quota Check)**: 在执行消耗资源的操作前，验证用户用量是否已达到配额限制的过程。
*   **限制体验 (Limit Experience)**: 用户达到配额限制时，系统所呈现的界面反馈和交互流程。
*   **降级 (Downgrade)**: 用户从 Pro 会员状态变为免费会员状态的过程（例如订阅到期未续费、主动取消）。
*   **锁定 (Locking)**: 在降级场景下，对超出免费配额的用户数据（如提示词）进行只读、不可用处理的状态。

## 4. 功能需求

### 4.1 配额管理 (`QuotaService`)

**4.1.1 配额定义与存储**

*   **数据模型**:
    *   `QuotaLimits`: 定义不同会员等级 (`MembershipStatus`: 'free', 'pro') 下各项资源的限制值。
        *   `storageLimit`: 提示词最大存储数量 (free: 5, pro: 100)
        *   `dailyOptimizationLimit`: 每日最大优化次数 (free: 3, pro: 50)
        *   *(未来可扩展其他资源限制)*
    *   `QuotaUsage`: 存储每个用户各项资源的当前用量。
        *   `userId`: 用户唯一标识
        *   `promptCount`: 当前存储的提示词数量
        *   `optimizationsToday`: 今日已使用的优化次数
        *   `lastOptimizationReset`: 上次优化次数重置的时间戳
*   **存储**: 配额用量数据 (`QuotaUsage`) 需要持久化存储（例如 Firestore 中的用户文档内），并能与本地缓存同步。配额限制 (`QuotaLimits`) 可以作为配置存储。
*   **服务**: 创建 `QuotaService`，负责配额相关的核心逻辑。

**4.1.2 配额初始化与更新**

*   新用户注册时，初始化其 `QuotaUsage` 数据（用量为 0）。
*   用户的 `QuotaUsage` 应在执行相关操作时实时更新（例如，保存提示词后 `promptCount` 增加，执行优化后 `optimizationsToday` 增加）。

**4.1.3 配额重置机制**

*   **每日优化次数重置**:
    *   `QuotaService` 需要包含一个定时机制（或在相关检查时触发判断）。
    *   在每日凌晨 00:00（基于用户本地时区，或者统一使用 UTC 时间）将所有用户的 `optimizationsToday` 重置为 0，并更新 `lastOptimizationReset` 时间戳。
    *   重置逻辑需要健壮，能处理跨天、服务重启等情况。

**4.1.4 与会员状态联动 (`MembershipService` 集成)**

*   `QuotaService` 必须能够感知用户的当前会员状态 (`MembershipService.getCurrentMembership()`)。
*   所有配额检查逻辑必须基于用户当前的会员状态，从 `QuotaLimits` 获取对应的限制值。
*   当用户会员状态发生变化时（升级/降级），`QuotaService` (或 `MembershipService` 在状态变更后通知 `QuotaService`) 需要能够触发相应的处理逻辑（特别是降级时的超额数据处理，见 4.3）。

### 4.2 配额检查与限制应用

**4.2.1 检查时机**

*   **存储配额检查**: 在用户尝试 **保存新的提示词** 或 **导入提示词** 操作 **之前** 执行。
*   **优化次数配额检查**: 在用户尝试 **执行优化操作** **之前** 执行。

**4.2.2 检查逻辑**

*   `QuotaService` 提供检查接口，例如：
    *   `canAddPrompt(): Promise<boolean>`: 检查当前用户是否可以添加新的提示词。
    *   `canOptimizePrompt(): Promise<boolean>`: 检查当前用户是否可以执行优化。
*   检查逻辑需要比较用户当前的 `QuotaUsage` 和其会员等级对应的 `QuotaLimits`。

**4.2.3 达到限制时的行为**

*   **阻止操作**: 如果检查结果为 `false` (达到限制)，必须阻止用户执行相应的操作（例如，保存按钮置灰或点击无效，优化按钮置灰或点击无效）。
*   **用户反馈**:
    *   **首次达到限制**: 触发显示相应的**配额限制横幅 (QuotaBanner)** (见 5.1)。
    *   **非首次**: 如果横幅已被关闭，则在用户尝试执行被阻止的操作时，可以显示一个简短的 Toast 提示或模态框，说明原因并引导升级（例如："免费版提示词存储已达上限，升级 Pro 解锁更多空间。"）。
*   **错误处理**: 配额检查相关的错误（例如，无法读取用量数据）应被妥善处理，并向用户显示友好的错误提示，避免阻塞核心流程。考虑提供重试机制。

### 4.3 降级处理 (`handleDowngrade`)

**4.3.1 触发时机**

*   当 `MembershipService` 检测到用户会员状态从 `pro` 变为 `free` 时，应调用 `QuotaService` 的降级处理逻辑。

**4.3.2 超额数据处理策略**

*   **仅处理存储配额**: 降级处理主要针对提示词存储数量。优化次数等周期性配额会在下次重置时自动符合免费版限制。
*   **锁定机制**:
    *   检测用户当前的 `promptCount` 是否超过免费版的 `storageLimit` (5条)。
    *   如果超额，**不删除**任何提示词。
    *   **锁定**超出免费限额的最**旧**的提示词，**保留**最新的 5 条提示词为可用状态。例如，用户有 10 条提示词，降级后，按创建时间排序，最早创建的 5 条将被锁定，最新创建的 5 条保持可用。
    *   被锁定的提示词在数据层面应标记为 `locked: true`。

**4.3.3 用户通知**

*   在降级处理完成后，应通过 UI 通知用户其部分提示词已被锁定。
    *   可以显示一个一次性的全局通知/横幅，说明情况（例如："您的 Pro 会员已到期。为符合免费版限制，部分较早的提示词已被暂时锁定。升级 Pro 可重新访问所有提示词。"）。
    *   通知应包含明确的升级引导按钮。

**4.3.4 重新访问 (升级后)**

*   当用户重新升级到 Pro 版后，`MembershipService` 应通知 `QuotaService`。
*   `QuotaService` 需要将该用户所有被锁定的提示词 (`locked: true`) 解锁 (`locked: false`)。

### 4.4 分析埋点 (可选但推荐)

*   记录用户达到配额限制的事件（区分存储/优化）。
*   记录用户点击配额限制横幅/锁定卡片上升级按钮的事件。
*   记录降级事件和锁定处理事件。
*   这些数据有助于分析免费用户的限制接触点和升级转化。

## 5. UI/UX 设计

**适配性说明**: 本章节定义的 UI 元素尺寸（如横幅高度、字体大小、图标大小）已考虑 Chrome 扩展侧边栏（常见宽度约 300px-400px）或弹出窗口的有限空间。实现时需注意确保在不同宽度下的布局合理性与响应式表现。特别是 `LockedPromptCard` 的悬停交互，需在有限的卡片空间内精心实现，避免拥挤。`Usage Display` 的适配性依赖于其容器（如 `MembershipCenter`）的最终宽度。

本章节详细描述配额限制和降级锁定相关的用户界面元素和交互体验，参考 `payment.md` 中的设计原则，力求清晰、一致、友好。

### 5.1 配额限制横幅 (`QuotaBanner`)

*   **目的**: 在用户首次达到免费配额限制时，以非干扰方式清晰告知，并提供升级入口。
*   **参考设计**: 类似 GitHub/Google Cloud 的顶部通知横幅，信息清晰，操作直接。
*   **触发条件**: 用户执行操作触发配额检查，且检查结果为首次达到该项配额限制时显示。
*   **位置**: 固定在主内容区域（如提示词列表）的**顶部**，不随页面滚动而消失 (sticky)，确保可见性。
*   **视觉样式**:
    *   **布局**: 水平条状布局，高度固定为 `48px`。
    *   **背景**: 使用应用主题内的浅色、柔和警示色调。例如，淡黄色 (`#FFFBEA`) 或淡蓝色 (`#EBF5FF`)，带有非常细微的顶部到底部渐变以增加质感。边框使用 1px 的同色系更深颜色（例如 `#FADF8E` 或 `#B3D9FF`）。
    *   **图标**: 左侧放置一个相关的提示图标，例如信息图标 (`ⓘ`) 或警告三角图标 (`⚠`)，颜色与文本颜色一致或稍深，尺寸约 `18px`。
    *   **文字**: 使用应用的标准 UI 字体（如 sans-serif），`14px` 大小，颜色为对比度足够的中性深色（如 `#5E6A7D`）。文本内容简洁明了，如：
        *   存储限制："免费版最多存储 5 条提示词，您已达到上限。"
        *   优化限制："免费版每日可优化 3 次，今日次数已用完。"
    *   **升级按钮 (CTA)**: 位于横幅右侧区域。样式参考 `payment.md` 中的 `UpgradeButton` 或主 CTA 按钮规范：
        *   尺寸：固定高度 `32px`，宽度根据文字自适应，左右 padding `16px`。
        *   背景：使用品牌主色调（如 `#4A90E2`）或醒目的强调色。
        *   文字：白色或高对比度浅色，`14px` 粗体，如 "升级 Pro"。
        *   圆角：`4px` 或与应用内其他按钮一致。
        *   Hover 状态：背景色加深 10% 或有轻微放大效果 (`scale: 1.03`)。
    *   **关闭按钮**: 位于最右侧，使用清晰的 `X` 图标，尺寸 `16px`，颜色与文本颜色一致，提供足够的点击区域 (`24px x 24px`)。Hover 状态图标背景变灰或图标本身变亮。
*   **交互与动效**:
    *   **出现**: 从内容区域顶部**平滑滑下**或**淡入**，动画时长 `200ms`，缓动函数 `ease-out`。
    *   **点击"升级 Pro"**: 跳转到官网付费页面 (`payment.md` 中定义的 URL)，在新标签页打开。
    *   **点击关闭按钮**: 横幅**平滑滑出**或**淡出**消失，动画时长 `150ms`。
        *   关闭后，该**特定类型**的配额横幅在当前会话或指定时间内（例如 24 小时）不再自动显示，避免过度打扰。但若用户后续触发**不同类型**的配额限制，则会显示对应的新横幅。
        *   即使用户关闭了横幅，后续尝试执行受限操作时，仍应通过 Toast 或其他非侵入方式提示原因。
    *   **响应式**: 在小屏幕下，按钮文字可考虑省略，仅保留图标和必要提示。

### 5.2 锁定提示词卡片 (`LockedPromptCard`)

*   **目的**: 在用户降级后，清晰地标识出超出免费配额而被锁定的提示词，并引导用户升级以恢复访问。
*   **参考设计**: Notion 的锁定页面元素，明确传达限制，但保留内容识别度。
*   **触发条件**: 用户为免费版，且该提示词数据被 `QuotaService` 标记为 `locked: true`。
*   **视觉样式**:
    *   **基础卡片**: 保持与普通提示词卡片相同的布局和尺寸。
    *   **遮罩层**: 在卡片内容之上覆盖一个半透明图层。
        *   颜色：黑色 (`#000000`)。
        *   透明度：`50% - 60%`，确保下方内容隐约可见但明显变暗。
    *   **锁定图标**: 在遮罩层**中央**显示一个清晰、识别度高的"锁"形图标。
        *   图标样式：实心填充图标。
        *   颜色：白色 (`#FFFFFF`) 或浅灰色，与深色遮罩形成对比。
        *   尺寸：根据卡片大小调整，例如 `24px` 或 `28px`。
        *   效果：可带有轻微的投影或描边，增强立体感。
    *   **内容可见性**: 卡片的标题或其他关键识别信息应在遮罩下仍能部分辨认，帮助用户知道是哪条提示词被锁定。
*   **交互与动效**:
    *   **Hover 效果**: 鼠标悬停在锁定的卡片上时：
        *   **遮罩层**: 透明度可轻微增加（例如从 50% 到 65%），或添加细微的内发光效果，提供视觉反馈。过渡动画时长 `150ms`。
        *   **浮现元素**: 在卡片**上方或下方**（根据可用空间）平滑浮现一个小的提示条/工具提示 (Tooltip)。
            *   提示条背景：深灰色或应用主色调的深色变种。
            *   提示文字：白色或浅色，`12px-14px`，内容为："升级 Pro 重新访问此提示词"。
            *   **升级按钮**: 在提示条旁边或下方出现一个与 `QuotaBanner` 中样式一致的小型"升级"按钮。
        *   **鼠标光标**: 变为 `not-allowed` 样式。
        *   **动画**: 浮现元素的出现应有 `100ms` 左右的淡入或轻微位移动画。
    *   **点击卡片**: 阻止执行默认的卡片点击行为（如打开详情）。
        *   反馈：可以触发卡片轻微震动一下或闪烁一次（动画时长 `200ms`）。
        *   同时，可以显示一个居中的模态框 (Modal) 或屏幕下方的 SnackBar/Toast，内容包括：
            *   标题："提示词已锁定"
            *   正文："此提示词超出了免费版存储限制。升级到 Pro 版即可访问所有提示词。"
            *   按钮：一个主要的"升级 Pro"按钮和一个次要的"取消"或"知道了"按钮。
    *   **点击卡片上浮现的"升级"按钮**: 跳转到官网付费页面。
*   **列表中的表现**: 锁定的卡片在提示词列表中应正常显示和排序，不应改变其相对位置。

### 5.3 用量展示 (在会员中心或其他设置区域)

*   **目的**: 向用户透明地展示其资源使用情况和当前限制。
*   **位置**: 建议在 `payment.md` 定义的 `MembershipCenter` (会员中心) 组件内实现。
*   **视觉样式**:
    *   **布局**: 在会员中心内创建一个专门的"用量概览"或"资源使用"区域。
    *   **存储用量**: (例如：37 / 100)
        *   使用**进度条 (Progress Bar)** 结合**数字标签**进行展示。
        *   进度条颜色可以使用品牌色或表示"消耗"的中性色（如蓝色、绿色）。
        *   当用量接近或达到上限时（如 > 80%），进度条颜色可变为警示色（黄色或橙色）。
        *   标签清晰显示"已用 / 总量"，例如："提示词存储: 37 / 100"。
    *   **每日优化用量**: (例如：12 / 50)
        *   由于是周期性重置，使用简单的**数字文本标签**即可。
        *   标签清晰显示"今日已用 / 每日总量"，例如："今日优化次数: 12 / 50"。
        *   可在旁边添加一个小的刷新图标或文字说明，提示"每日 UTC 0 点重置"。
    *   **Pro 专属资源**: 对于 Pro 会员独有的无限量或高额度资源，可以直接显示"无限"或对应的额度值，并用 Pro 标识突出。
*   **交互**: 用量信息为只读展示，但如果接近限额，可考虑在旁边放置一个不显眼的"升级"链接或提示。

## 6. 非功能性需求

*   **性能**: 配额检查必须快速完成，不能影响用户操作的响应速度。对数据库的读写应优化。
*   **可靠性**: 配额计算、重置、状态同步必须准确无误。降级处理不能丢失用户数据。
*   **可扩展性**: 设计应考虑未来可能增加新的受配额限制的资源。
*   **一致性**: 限制相关的 UI (横幅、锁定卡片、提示信息) 风格应保持一致，并符合整体应用的视觉设计规范。

## 7. 未来考虑

*   **更细化的配额**: 例如，按提示词类别、按使用功能模块限制。
*   **用量预警**: 在用户接近配额上限（例如达到 80%）时进行提示。
*   **团队版配额**: 如果未来推出团队版，需要设计团队整体的配额管理。

---
