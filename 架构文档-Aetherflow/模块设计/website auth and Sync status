# 官网认证与状态同步 - 调试经验总结 (B8任务及相关问题)

问题记录:
  - 问题描述: 扩展跳转官网后，登录状态未同步。
    根本原因: 前后端域名不匹配。扩展生成的跳转URL域名(`.github.io`)与Cloud Run服务允许的请求来源域名(`.com`)不一致，导致跨域请求失败。
    核心解法: 统一交互域名。确保前端生成URL时使用后端服务期望的域名。
    关键思考点: 检查前后端交互时涉及的所有域名配置（CORS、URL生成）是否一致。

  - 问题描述: 网站调用Cloud Run转换Token失败，提示权限不足。
    根本原因: Cloud Run服务账号缺少创建签名令牌的权限。Firebase Admin SDK创建Custom Token需要调用底层的签名API，运行服务的账号没有被授予此权限。
    核心解法: 在IAM中为服务账号配置必要角色。需要去Google Cloud控制台为Cloud Run使用的服务账号添加 "Service Account Token Creator" 角色，使其能代表自己执行签名操作。
    关键思考点: 理解云服务间的交互不仅需要网络连接，还需要操作层面的权限，特别是涉及安全操作（如签名、创建令牌）时，要去IAM检查服务账号的角色。

  - 问题描述: 官网同步认证状态的加载提示是中文。
    根本原因: 代码硬编码了中文提示文本，未考虑国际化。
    核心解法: 统一UI语言。将用户可见的提示文本修改为产品目标语言（英文）。
    关键思考点: 确保面向用户的界面元素使用统一且符合目标市场的语言，避免硬编码特定语言文本。

  - 问题描述: 只有部分按钮跳转官网时会同步认证，其他按钮行为不一致。
    根本原因: 代码修改不彻底。修复认证同步的逻辑（生成带Token的URL）只应用在了`UpgradeButton`，未应用在`ProPlanCard`的按钮上。
    核心解法: 保证逻辑一致性。检查并确保所有触发相同用户流程（跳转官网进行升级）的入口都使用了统一的、修复后的代码逻辑。
    关键思考点: 修复一个流程中的问题时，要检查所有能进入该流程的入口点，确保修复是完整的。
