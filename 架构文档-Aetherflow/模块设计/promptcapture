# AetherFlow 提示词剪藏功能设计

## 1. 功能概述

提示词剪藏是一项核心功能，允许用户在浏览网页时快速选择并保存有价值的文本内容到提示词库。当用户遇到好的提示词或有用的文本片段时，无需手动复制粘贴，只需通过右键菜单一键保存，系统会自动处理内容格式化和标题生成。

## 2. 用户价值

- **便捷收集**：用户可以在浏览网页时即时剪藏有价值的提示词，无需中断工作流
- **灵感保存**：帮助用户建立个人知识库，随时保存灵感或有用内容
- **格式保留**：完整保留原始文本格式，包括段落、换行和特殊符号
- **智能分类**：自动生成描述性标题，便于后续查找和管理

## 3. 成功标准与验收测试

### 3.1 剪藏流程简便性

- **标准**：用户能够通过简洁的操作快速剪藏任何网页上的文本内容
- **验收测试**：
  - 用户选中文本后，右键菜单中显示清晰可见的"Aetherflow-剪藏到库"选项
  - 从选中文本到保存提示词完成的操作不超过2步（选中→右键点击保存）
  - 剪藏过程无需额外确认对话框，直接完成保存
  - 操作完成后用户收到明确的成功反馈（通知提示）

### 3.2 剪藏内容完整性

- **标准**：剪藏的内容完整保留原始文本的格式和结构
- **验收测试**：
  - 剪藏带有多个段落的文本，验证所有段落和换行符被正确保留
  - 剪藏包含特殊格式（如Markdown、代码片段）的文本，验证格式不被破坏
  - 剪藏包含特殊字符（如表情符号、非英文字符）的文本，验证字符正确保存
  - 对于超长文本（>10000字符），系统能够完整剪藏而不截断

### 3.3 标题生成智能性

- **标准**：系统自动为剪藏的内容生成描述性、有意义的标题
- **验收测试**：
  - 对于问题型文本，生成的标题应反映问题的核心内容
  - 对于指令型文本，标题应包含主要动词和目标对象
  - 对于代码片段，标题应反映代码的功能或类型
  - 生成的标题长度适中（5-40个字符），不过长也不过短
  - 标题生成时间不超过2秒，不阻塞用户操作

### 3.4 用户反馈即时性

- **标准**：系统为用户提供即时、清晰的操作反馈
- **验收测试**：
  - 剪藏成功后，页面上立即显示成功通知，持续2-3秒
  - 剪藏失败时，显示具体错误原因，并提供可能的解决方案
  - 通知样式符合网页整体风格，不过于突兀
  - 剪藏成功后，提示词库界面自动更新，显示新剪藏的内容

### 3.5 跨浏览器环境兼容性

- **标准**：功能在所有主要网页环境中保持一致可用
- **验收测试**：
  - 在常见AI平台（如ChatGPT、Claude、Bard）页面中正常工作
  - 在普通网页、社交媒体、文档类网站中正常工作
  - 在动态加载内容的网页中也能正确剪藏选中文本
  - 不同浏览器版本（Chrome最新3个版本）下保持功能一致

## 4. 边界条件与异常场景

### 4.1 内容脚本连接异常

- **标准**：当内容脚本无法正常连接时，系统能优雅降级并恢复功能
- **处理方式**：
  - 检测到内容脚本无响应时，尝试使用备选的Chrome API获取选中文本
  - 自动尝试恢复内容脚本连接，最多重试3次
  - 向用户展示连接问题警告，但仍尽可能完成剪藏操作
  - 提供明确的错误状态指示器，告知用户当前功能状态

### 4.2 选中文本为空或过大

- **标准**：系统能妥善处理空文本或极大文本的情况
- **处理方式**：
  - 检测到选中内容为空时，显示"选中内容为空"的提示，不执行保存
  - 对于超大文本（>100KB），发出警告但允许用户确认后继续保存
  - 当文本超过存储限制时，向用户提供部分保存选项
  - 提供分段剪藏建议，帮助用户处理特别长的内容

### 4.3 网络和存储异常

- **标准**：在网络或存储出现问题时，保护用户数据并提供恢复机制
- **处理方式**：
  - 检测到存储操作失败时，自动将内容暂存到临时区域
  - 网络中断时，将请求加入队列，待连接恢复后自动处理
  - 存储空间不足时，提示用户清理空间并提供一键清理选项
  - 系统自动定期检查并处理未完成的剪藏请求

### 4.4 页面结构复杂场景

- **标准**：在复杂结构的页面中仍能准确剪藏用户选中的内容
- **处理方式**：
  - 使用多种方法尝试获取选中文本，确保最高成功率
  - 对于动态变化的DOM结构，采用更健壮的文本选择检测方法
  - 针对特殊网站（如有文本保护的网站）提供备选剪藏方案
  - 在无法直接剪藏的情况下，提供手动输入选项

### 4.5 重复内容处理

- **标准**：系统能识别并适当处理重复的内容剪藏请求
- **处理方式**：
  - 检测到用户尝试保存已存在的内容时，提供更新或创建副本的选项
  - 对相似但不完全相同的内容，建议用户确认是否需要两份
  - 重复剪藏同一内容时，更新原条目的使用频率和最后使用时间
  - 提供智能合并选项，帮助用户整合类似的提示词

## 5. 用户界面与交互

### 5.1 右键菜单设计

- 清晰的菜单项文本："Aetherflow-剪藏到库"
- 仅在有文本选中时显示菜单项
- 菜单项位置保持在右键菜单的顶部区域，易于发现
- 支持快捷键操作（可选Ctrl+Shift+S）

### 5.2 成功通知设计

- 简洁的成功消息："提示词已成功剪藏到库中"
- 浮动通知显示在页面右上角，不干扰用户操作
- 通知包含轻微的动画效果，引起用户注意
- 通知自动消失，无需用户交互

### 5.3 错误通知设计

- 明确的错误信息，如"剪藏失败: [具体原因]"
- 错误通知使用不同颜色（红色）区分于成功通知
- 较长显示时间（5秒），确保用户有足够时间阅读
- 提供可能的解决建议或重试按钮

## 6. 与其他功能的集成

### 6.1 提示词库集成

- 剪藏的提示词自动显示在提示词库中的最前位置
- 新剪藏的提示词默认标记为"收藏"状态
- 提供一键访问最近剪藏提示词的快捷方式
- 支持在剪藏后立即编辑或优化提示词

### 6.2 快捷指令集成

- 新剪藏的提示词可立即通过快捷指令"/"调用
- 最近剪藏的提示词在快捷指令搜索结果中优先展示
- 支持为剪藏的提示词添加自定义快捷关键词

### 6.3 配额管理集成

- 剪藏功能考虑用户的存储配额限制
- 免费用户接近配额限制（已使用4/5）时显示警告
- 达到配额限制时提供明确的升级引导
- 付费用户享受更高的剪藏内容数量限制

## 7. 浮动工具栏设计方案

为了提供更直观、便捷的剪藏入口，替代或补充原有的右键菜单方案，我们设计了浮动工具栏功能。

### 7.1 功能 (Functionality)

- **触发方式:** 当用户在网页上通过鼠标选择一段文本后自动出现。
- **核心操作:** 提供三个固定的操作按钮：
    - **禁用按钮 (电源图标):** 左侧，用于触发"在此站点禁用 AetherFlow"的操作 (功能待实现)。
    - **捕获按钮 (书签+图标):** 中间，核心操作按钮。
    - **Logo按钮 (Sparkles图标):** 右侧，用于打开 AetherFlow 侧边栏。
- **后续流程:** 点击"捕获"按钮后，触发"剪藏预览与编辑"窗口；点击 Logo 按钮则打开侧边栏。
- **消失时机:**
    - 用户点击页面其他任意位置。
    - 用户取消文本选择。
    - 用户开始新的文本选择。
    - 剪藏操作完成（或取消）后。

### 7.2 用户体验 (UX)

- **可见性:** 工具栏应**紧邻用户鼠标抬起（完成选择）的位置**出现，通常在选区**垂直方向**的上方或下方（优先上方，若空间不足则在下方），避免遮挡过多原文。
- **响应性:** 工具栏的出现和消失应**即时**，避免用户等待，响应时间控制在100ms内。
- **简洁性:** 工具栏包含三个图标（电源、书签+、Sparkles），**不直接显示文字**，通过 Tooltip 提供各按钮的功能信息。
- **非侵入性:** 工具栏尺寸小巧，设计简洁（白色背景、品牌色图标），不应过多分散用户注意力。用户可以轻松忽略它，除非需要进行相关操作。
- **一致性:** 工具栏的视觉风格应与AetherFlow扩展的整体风格保持一致。

### 7.3 用户界面 (UI)

- **外观:**
    - 形状: 白色背景的药丸状 (`border-radius: 999px`)，内部包含三个圆形的按钮 (`border-radius: 50%`)。
    - 颜色: 背景为白色 (`#ffffff`)，图标使用AetherFlow的主要品牌色 (紫色, `--aetherflow-primary-bg`)。
    - 尺寸: 高度约30-34px (根据内边距和图标大小决定)，宽度根据内容自适应，但保持紧凑。
    - 阴影: 添加轻微的投影效果 (`box-shadow`)，使其与页面内容区分开。
- **内容:**
    - **图标组合 (从左到右):**
        - **禁用图标 (X 图标):** 代表"在此站点禁用"。
        - **操作图标 (BookmarkPlus):** 代表"捕获/添加到库"。
        - **AetherFlow Logo (Sparkles):** 代表"打开 AetherFlow 侧边栏"。
    - **Tooltip提示:** 
        - 鼠标悬停在 **X 图标** 上时，显示 "Turn off AetherFlow on this site"。
        - 鼠标悬停在 **BookmarkPlus 图标** 上时，显示 "Capture to AetherFlow"。
        - 鼠标悬停在 **Logo 图标** 上时，显示 "Open AetherFlow"。
- **动画:** *(移除动画效果，追求即时响应)*
    - 点击按钮: 轻微的背景色变化反馈效果 (`hover` 时背景变为浅灰色)。

### 7.4 实现方式建议 (Implementation)

- **技术选型:** 建议使用原生JavaScript在`content script`中手动实现，避免引入额外库依赖。
- **核心逻辑:**
    1.  **监听选择:** 在`content script`中监听`document`的`mouseup`事件。
    2.  **获取选区与事件位置:** 在`mouseup`事件回调中，使用`window.getSelection()`获取选中文本和范围(Range)，并记录`event.clientX`, `event.clientY`。
    3.  **判断条件:** 检查`selection.toString().trim()`是否为空。
    4.  **Shadow DOM 检测:** 检查选区的`commonAncestorContainer`是否位于某个`ShadowRoot`内部。
    5.  **计算位置 (相对末端定位):**
        -   获取选区最后一个Range的边界矩形：`range.getBoundingClientRect()` 用于判断垂直空间。
        -   **垂直位置:** 根据`rect.top`, `rect.bottom` 和工具栏高度，判断工具栏应显示在选区上方还是下方，计算 `top` 值 (结合 `window.scrollY`)。
        -   **水平位置:** 使用 `event.clientX` （结合 `window.scrollX`）作为基准，计算 `left` 值，使工具栏**水平居中于鼠标抬起的位置**（`left = event.clientX + window.scrollX - toolbar.offsetWidth / 2`）。
        -   **边界约束:** 确保计算出的 `top` 和 `left` 值在视口内，使用 `Math.max/min` 进行约束。
    6.  **创建/复用工具栏:**
        -   *(性能优化: 优先复用已存在的工具栏DOM实例)*
        -   动态创建`div`元素作为工具栏容器（如果实例不存在）。
        -   设置样式 *(样式隔离: 所有CSS类名使用特定前缀，通过`chrome.scripting.insertCSS`注入)*
        -   **创建按钮:** 在容器内创建三个`button`元素，分别包含 X, BookmarkPlus, Sparkles 图标。
        -   **附加节点:** 根据是否在 Shadow DOM 内决定附加到 `ShadowRoot` 或 `document.body`。
    7.  **绑定事件:**
        -   给 **X 图标按钮** 绑定`click`事件，触发 `handleDisableSiteClick`。
        -   给 **BookmarkPlus 图标按钮** 绑定`click`事件，触发 `handleCaptureClick` (打开预览编辑窗口)。
        -   给 **Logo 图标按钮** 绑定`click`事件，触发 `handleLogoClick` (打开侧边栏)。
        -   使用按钮的 `title` 属性提供 Tooltip。
        -   监听`document`的`mousedown`事件，隐藏工具栏。
    8.  **管理状态:** 记录工具栏实例及其状态。
- **注意事项:**
    -   Shadow DOM, iframe, 性能, 样式隔离, MV3 Service Worker (如前所述)。

## 8. 剪藏预览与编辑窗口设计方案

当用户点击浮动工具栏上的"剪藏"按钮后，将弹出此窗口，提供预览、编辑和确认保存的功能。

### 8.1 功能 (Functionality)

- **窗口标题:** 在窗口顶部显示明确标题，如 "Save to AetherFlow"。
- **内容预览:** 清晰展示用户选中的文本内容。
- **标题编辑:**
    - 显示**临时标题**（如内容前50字符）。
    - **异步生成智能标题:** 后台根据内容生成标题。
    - **加载状态显示:** UI上指示后台正在生成标题。
    - **用户优先更新:** 只有当用户未修改临时标题时，才使用后台返回的智能标题更新输入框。
    - 允许用户随时编辑或替换标题。
- **内容编辑:** 允许用户对剪藏内容进行简单修改。
- **Pin (固定) 功能:** 提供 Pin 按钮，允许用户将窗口固定在页面上，点击外部区域不关闭。
- **保存操作:** "保存"按钮，将最终标题和内容存入提示词库。
- **取消/关闭操作:** "关闭 (X)" 按钮，放弃操作并关闭窗口。
- **未来扩展:**
    - 分类/标签 (以后增加)
    - 元数据展示 (以后增加)
    - 提示词优化按钮 (以后增加)

### 8.2 用户体验 (UX)

- **触发与定位:** 点击浮动工具栏后快速、无动画弹出，居中模态框。
- **交互模式:** 默认为模态框。点击 Pin 按钮后切换为**非模态**，窗口保持可见，允许与页面其他部分交互；再次点击取消 Pin 恢复模态。
- **清晰度:** 明确的窗口标题告知用户用途。界面布局清晰，各区域明确。
- **流程效率:**
    - **立即响应:** 窗口立即弹出并显示临时标题。
    - **异步智能:** 后台生成标题不阻塞用户编辑，加载指示器提供反馈。
    - **用户控制:** 用户编辑优先于自动更新；Pin 功能提供操作灵活性。
    - **快速保存:** 保存操作迅速，窗口关闭。
- **美观与轻量:** 
    - 采用更协调的边距与间距，提升呼吸感。
    - 使用清晰易读的字体和合适的字号。
    - 整体保持浅色背景，品牌色作为点缀（标题、按钮、焦点、加载指示器），避免视觉干扰。
    - 使用柔和的圆角和阴影。
- **反馈机制:** 保存成功/失败时提供明确反馈；加载状态有指示器。
- **可扩展性:** 设计预留空间。

### 8.3 用户界面 (UI)

- **窗口样式:**
    - 轻量级模态对话框，柔和视觉风格，圆角、细微阴影。
    - 尺寸紧凑，配色浅色为主，品牌色点缀。
- **布局结构:**
    - **头部 (Header):** 
        - 左侧: 窗口标题 "Save to AetherFlow" (可使用品牌色或深灰色)。
        - 右侧: 窗口控件按钮 (Pin/PinOff 图标按钮, 关闭(X) 图标按钮)。
    - **主体 (Body):** (增加间距 `gap`)
        - "Title" 标签。
        - 文本输入框 (右侧内部或旁边预留**加载指示器(Spinner)**位置)。
        - "Content" 标签。
        - 多行文本区域 (`textarea`)。
    - **(未来扩展区域):** ...
    - **底部 (Footer):** 右对齐 "保存" 按钮 (品牌强调色)。
- **控件细节:**
    - Pin 按钮根据状态切换图标 (Pin / PinOff)。
    - 加载指示器 (Spinner) 样式小巧、灰色，不突兀。
    - 输入框、文本区域、按钮样式统一、简洁。
- **可扩展性:** 灵活布局。

### 8.4 技术实现考量 (Technical Implementation)

- **窗口实现:** 推荐方案：Content Script 动态创建 DOM。
- **通信 (MV3):** Content Script <-> Background (如前所述)。后台需增加处理"生成标题"请求的逻辑。
- **状态管理 (MV3):** 
    - Content Script 管理窗口显隐、**固定(pinned)状态**、表单内容、**标题加载状态**。
    - Background 处理保存、标题生成。
- **Pin 功能:** 实现切换模态/非模态行为，调整外部点击关闭逻辑。
- **异步标题 UI:** 
    - 实现 Spinner 的显示/隐藏逻辑。
    - 实现条件更新标题逻辑（检查用户是否已编辑）。
- **可扩展性:** 模块化代码。
- **CSP与样式隔离:** (如前所述)。

## 9. 未来交互统一计划

为了确保用户在通过不同入口点（如右键菜单、浮动工具栏）进行剪藏操作时获得一致且可预测的体验，计划在浮动工具栏及"剪藏预览与编辑"窗口功能稳定实现后，对原有的右键菜单交互流程进行调整。

- **目标:** 统一所有剪藏入口的行为。
- **调整内容:** 用户通过右键菜单选择"Aetherflow-剪藏到库"选项后，**不再直接保存**，而是**同样触发第8节所描述的"剪藏预览与编辑"窗口**。
- **收益:** 提升用户体验一致性，确保所有剪藏内容的功能一致性（如编辑、未来标签等），并促进代码复用。
