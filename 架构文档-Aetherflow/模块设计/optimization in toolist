# AetherFlow 工具栏快速优化功能设计

## 1. 功能目标

在用户浏览和编辑网页时，提供一种**快速、轻量、低干扰**的文本优化入口。用户选中文字后，可直接通过浮动工具栏触发优化，并在一个小型、非模态的弹窗中快速查看和复制优化结果，**最大限度减少对用户当前主要任务（如写作、阅读）心流的打断**。此功能服务于"辅助写作"场景，区别于"剪藏前精炼"场景。

## 2. 用户流程

1.  **选择文本:** 用户在任何网页上用鼠标选中一段需要优化的文本。
2.  **工具栏出现:** 浮动工具栏在选区附近（或鼠标释放位置）自动出现。
3.  **点击优化:** 用户点击工具栏上新增的"优化"图标按钮 (✨ Wand2)。
4.  **后台处理:** 前端向后台发送优化请求，并显示加载状态（可选，例如按钮暂时禁用或显示微小加载指示）。
5.  **结果弹窗:** 优化完成后，在工具栏附近或选区附近弹出一个**新的、轻量级的优化结果小窗口**。
6.  **查看与复制:** 用户在小窗口中查看优化后的文本，并可点击"复制"按钮复制代码。
7.  **关闭弹窗:** 用户点击"复制"按钮后，或点击小窗口的关闭按钮，或点击页面其他任何位置，小窗口自动关闭。

## 3. UI/UX 设计

### 3.1 浮动工具栏按钮

-   **新增按钮:** 在现有的浮动工具栏中，根据新的顺序添加"优化"按钮。
-   **图标:** 使用 **魔法棒 (Magic Wand)** 图标 (具体图标库待定，需选择一个清晰简洁的魔法棒样式) 代表优化功能。
-   **样式:** 与工具栏上其他按钮保持完全一致：
    -   圆形 (`border-radius: 50%`)
    -   背景透明，图标颜色使用品牌紫色 (`--aetherflow-primary-bg`)
    -   尺寸、内边距与现有按钮相同。
    -   悬停时显示浅灰色背景 (`--aetherflow-toolbar-hover-bg`)。
-   **最终按钮顺序 (从左到右):**
    1.  **关闭 (X):** 禁用当前站点功能。
    2.  **剪藏 (BookmarkPlus):** 捕获并打开预览编辑窗口。
    3.  **优化 (Magic Wand):** 触发快速优化流程及小窗口。
    4.  **Logo (Sparkles):** 打开 AetherFlow 侧边栏。
-   **交互:**
    -   点击优化按钮后触发优化流程。
    -   **Tooltip:** 鼠标悬停在优化按钮上时显示提示文字，如 "Optimize selection"。
    -   *(可选)* 点击后、后台处理优化时，按钮可显示禁用状态或微小的加载指示。

### 3.2 轻量级优化结果弹窗 (新 UI 元素)

-   **定位与行为:**
    -   **非模态 (Non-modal):** 出现时不阻止用户与背景页面交互。
    -   **定位:** 出现位置应靠近触发点（浮动工具栏或选区末端），**不居中**，避免遮挡用户视线焦点。例如，可以尝试出现在工具栏正下方或右下方。
    -   **关闭:** 点击弹窗外部区域、点击"复制"按钮、点击弹窗上的关闭按钮时，弹窗应平滑消失。
-   **外观:**
    -   **尺寸:** 显著小于"剪藏预览编辑"窗口，力求小巧紧凑。
    -   **形状:** 圆角矩形，白色背景 (`--aetherflow-modal-bg`)。
    -   **阴影:** 添加非常轻微的阴影 (`box-shadow`)，以区分背景。
    -   **配色:** 整体白色，文字使用深灰色 (`--aetherflow-text-dark`)，复制按钮可使用品牌色或灰色。
-   **内容与布局 (极简):**
    -   **头部 (可选，极简):** 可包含一个小标题"Optimized Text"或仅包含一个关闭 (X) 图标按钮在右上角。
    -   **主体:**
        -   显示优化后的文本内容。
        -   如果文本过长，主体区域应**允许垂直滚动** (`overflow-y: auto`)，并限制最大高度。
    -   **底部:**
        -   放置一个清晰的 **"复制" (Copy) 图标按钮**，方便用户快速获取结果。
-   **交互:**
    -   **无编辑功能:** 弹窗内的文本不可编辑。
    -   **复制反馈:** 点击复制按钮后，可提供短暂的视觉反馈（如图标变为"已复制"√ 或显示"Copied!"文字）。

## 4. 技术实现考量

### 4.1 核心技术选型与决策 (新增)

*   **LLM 选型:** 统一将优化功能（包括工具栏快速优化和现有提示词优化）的后端 LLM 从 DeepSeek 更换为**豆包 Lite (`doubao-lite-32k-240828`)**，API 端点为 `https://ark.cn-beijing.volces.com/api/v3/chat/completions`，以提高响应速度。
*   **API Key 管理:** 
    *   移除所有硬编码的 API Key。
    *   采用**安装时写入 `chrome.storage.local`** 的方式存储豆包 API Key (`32550ef8-b626-4478-bf53-5fb5e34e114f`)，存储键名使用 `doubaoApiKey`。
    *   所有调用豆包 API 的代码必须从 `storage` 异步读取 Key，并处理 Key 不存在的情况。
*   **统一服务入口与模块化 (更新):** 
    *   **所有**提示词优化相关的后端 API 调用逻辑将**集中**在 `optimizationService.ts` 中处理，提供统一的服务接口（如 `optimizePrompt`）。
    *   为了提高可维护性，计划将包含系统提示词内容的 `getSystemPrompt` 函数**拆分**到新的 `extension/src/services/systemPrompts.ts` 文件中。
*   **系统提示词管理 (更新):** 
    *   `getSystemPrompt` 函数（位于 `systemPrompts.ts`）将管理**四种**不同的提示词模式，以服务不同功能入口：
        *   `'standard'`: 标准模式，用于侧边栏。
        *   `'creative'`: 创意模式，用于侧边栏。
        *   `'concise'`: 简洁模式，用于侧边栏。
        *   `'universal'`: **通用模式**，用于工具栏快速优化和剪藏预览窗口优化。此模式将使用本文档**附录**中经过测试验证的 V3 通用优化提示词内容。
*   **接口调用策略 (更新):** 
    *   **侧边栏优化 (`OptimizeSection.tsx`)** 将根据用户选择，调用 `optimizationService.ts` 中的函数，并传递对应的模式 (`'standard'`, `'creative'`, `'concise'`)。
    *   **工具栏快速优化 (`background/index.ts` 处理 `OPTIMIZE_SELECTION`)** 将**重构**为调用 `optimizationService.optimizePrompt(text, 'universal')`，不再保留独立的 API 调用逻辑。
    *   **剪藏预览窗口优化 (`capture.ts` -> `background/index.ts`)** (阶段七实现时) 也将调用 `optimizationService.optimizePrompt(text, 'universal')` (或根据需要选择其他模式)。

### 4.2 具体实现细节 (原内容调整归类)

1.  **浮动工具栏 (`floatingToolbar.ts`, `toolbar.css`):** (原 4.1)
    *   在 `getOrCreateFloatingToolbar` 中添加新的"优化"按钮。
    *   更新 CSS (`toolbar.css`) 以调整按钮顺序。
2.  **优化结果弹窗 (`optimizationPopup.ts`, `optimizationPopup.css`):** (原 4.2)
    *   DOM 构建、显隐控制、定位逻辑。
    *   点击外部关闭逻辑 (在 `capture.ts` 的 `handleMouseDown` 中)。
    *   复制按钮交互及反馈。
3.  **消息通信 (`floatingToolbar.ts`, `optimizationPopup.ts`, `background/index.ts`):** (原 4.3)
    *   **内容脚本 (`floatingToolbar.ts`):** 点击优化按钮时，获取选中内容，向后台发送 `{ type: 'OPTIMIZE_SELECTION', payload: { content: selectedText } }` 消息。
    *   **后台脚本 (`background/index.ts`):**
        *   添加对 `OPTIMIZE_SELECTION` 消息的监听。
        *   读取 `doubaoApiKey`。
        *   调用豆包 API (使用 `concise` 模式系统提示和 Lite 模型)。
        *   通过 `chrome.tabs.sendMessage` 将结果 `{ type: 'OPTIMIZATION_RESULT', payload: { optimizedContent: resultText } }` 发送回请求的 Tab。
    *   **内容脚本 (`optimizationPopup.ts`):** 添加对 `OPTIMIZATION_RESULT` 消息的处理，后续用于更新弹窗内容。
4.  **优化服务重构 (`optimizationService.ts`, `background/index.ts`):** (原 4.4 及新增)
    *   **`optimizationService.ts` 重构:** 
        *   移除 DeepSeek API Key 和 URL 硬编码。
        *   修改 `makeAPIRequestWithRetry` 或其调用处，改为使用 `fetch` 调用豆包 API。
        *   API Key 获取改为从 `chrome.storage.local` 读取 `doubaoApiKey`。
        *   模型 ID 使用 `doubao-lite-32k-240828`。
    *   **后台脚本 (`background/index.ts`):**
        *   **安装时写入 Key:** 在 `onInstalled` 事件中，将 API Key `32550ef8-b626-4478-bf53-5fb5e34e114f` 写入 `chrome.storage.local.set({ doubaoApiKey: '...' })`。
        *   **可能需要引入 `getSystemPrompt`:** 如果处理 `OPTIMIZE_SELECTION` 需要直接使用简洁模式提示词，可能需要从 `optimizationService.ts` 导入 `getSystemPrompt` 函数或复制其简洁模式部分。

## 5. 与剪藏优化功能的区分

-   **入口分离:** 工具栏优化按钮 vs 预览窗口内优化链接。
-   **UI 分离:** 轻量级结果弹窗 vs 功能完整的预览编辑窗口。
-   **交互分离:** 快速复制 vs 编辑后保存。

确保两种优化流程在代码层面和用户体验层面都清晰分离，服务于各自的目标场景。

## 6. 实施计划 (分阶段)

为了更好地管理开发过程，将工具栏快速优化功能的实现拆分为以下阶段：

1.  **阶段一：工具栏 UI 更新**
    *   **任务:** 在浮动工具栏中添加新的"优化"（魔法棒）按钮，并调整按钮顺序为：关闭(X) -> 剪藏(BookmarkPlus) -> 优化(Magic Wand) -> Logo(Sparkles)。
    *   **涉及文件:** `capture.ts` (HTML结构), `captureUI.css` (样式与顺序)。
    *   **验收标准:** 工具栏正确显示四个按钮，顺序符合设计。

2.  **阶段二：轻量级优化结果弹窗 UI 构建**
    *   **任务:** 创建新的、独立的优化结果弹窗的 HTML 结构和 CSS 样式（布局、定位、滚动条、按钮等）。
    *   **涉及文件:** `capture.ts` (DOM构建逻辑), `captureUI.css` (样式规则)。
    *   **验收标准:** 弹窗的基本视觉外观完成，符合设计稿。

3.  **阶段三：弹窗显示与基础交互**
    *   **任务:** 实现优化结果弹窗的显示、隐藏逻辑，以及基于触发点的定位计算和点击外部关闭功能。
    *   **涉及文件:** `capture.ts` (显隐/定位函数, `handleMouseDown` 更新)。
    *   **验收标准:** 可以通过调用函数弹出和关闭样式正确的空弹窗，并出现在预期位置。

4.  **阶段四：前后端通信与优化逻辑触发**
    *   **任务:** 建立内容脚本和后台脚本之间的消息传递机制 (`OPTIMIZE_SELECTION`, `OPTIMIZATION_RESULT`)，后台调用（占位的）优化服务。
    *   **涉及文件:** `capture.ts` (发送/接收消息), `background/index.ts` (接收/发送消息, 调用服务)。
    *   **验收标准:** 点击工具栏优化按钮能触发后台逻辑，后台能将模拟结果发回内容脚本。

5.  **阶段五：结果展示与核心流程打通**
    *   **任务:** 将后台返回的优化结果填充到轻量级弹窗中进行展示。
    *   **涉及文件:** `capture.ts` (在消息回调中更新弹窗内容)。
    *   **验收标准:** 完成核心流程：选中 -> 点击优化 -> 弹窗显示优化结果。

6.  **阶段六：完善弹窗交互 (复制与关闭)**
    *   **任务:** 实现弹窗上的"复制"按钮功能、复制反馈，并确保所有关闭条件（点击复制、点击关闭按钮、点击外部）都能正常工作。
    *   **涉及文件:** `capture.ts` (事件绑定与处理)。
    *   **验收标准:** 轻量级优化弹窗功能完整、交互顺畅。

7.  **阶段七：集成优化至剪藏预览窗口 (方案 B)**
    *   **任务:** 在现有的"剪藏预览与编辑"窗口的内容区域下方，添加"优化内容"(✨ Optimize Content) 触发器（文字链接或图标按钮）。实现点击触发器后调用后台优化服务，在窗口内显示加载状态，并用优化结果替换原始内容。
    *   **涉及文件:** `capture.ts` (添加触发器DOM, 新的点击处理函数, 消息通信, 加载状态处理, 内容替换), `captureUI.css` (触发器样式), `background/index.ts` (处理新的优化消息类型)。
    *   **验收标准:** 用户能在预览编辑窗口中通过新触发器优化当前内容，并看到内容被替换。

## 附录：工具栏快速优化当前使用的系统提示词 (通用优化 v3)

```text
# 提示词优化专家指南 (通用优化 v3)

## 核心任务
你是一位专业的提示词优化专家。你的唯一任务是分析和改进用户提供的**提示词**（用户打算发给其他 AI 的指令），目标是提升该提示词的**清晰度、结构性、明确性、完整性和整体效果**，从而帮助用户从目标 AI 获得更高质量、更符合预期的回复。

**请牢记：你的最终输出必须仅仅是优化后的提示词文本本身，不包含任何其他文字。**

## 语言要求
- **严格保持**输出语言与待优化提示词的语言一致（中文输入则中文输出，英文输入则英文输出）。
- **禁止**在优化过程中改变原始语言。

## 输出要求 (极其重要 - 再次强调)
1.  **直接输出优化后的提示词文本。**
2.  输出内容**仅仅**是优化过的、可以直接复制使用的提示词本身。
3.  **绝对禁止**包含任何引导语（如 "优化后的提示词:", "这是优化版本:", "Optimized prompt:" 等）。
4.  **绝对禁止**包含任何自定义标签（如 `【核心任务】`, `【关键约束】` 等）。
5.  **绝对禁止**包含任何解释、评论、分析或元说明文字。
6.  **允许**在优化后的提示词**内部**使用标准的 Markdown 格式（例如列表 `-`, `*`, `1.`, 粗体 `** **`）来增强提示词自身的结构和可读性，前提是这样做能提升提示词的质量和效果。

## 优化原则与方法（内部指导）
优化时请遵循以下原则（按重要性排序），这些是你的思考框架，**不要在输出中体现**：
1.  **保持原意**: 必须完整保留用户提示词的原始核心意图和目标。这是最高优先级。
2.  **内部思考步骤 (推荐)**:
    *   第一步：理解原始提示词。它的核心目标是什么？预期受众 AI 是谁？上下文是什么？
    *   第二步：评估其质量。优点是什么？缺点（模糊、缺少信息、结构混乱、过于复杂/简单等）是什么？
    *   第三步：构思改进策略。应用哪些优化原则（明确性、结构、完整性、效率）？是否需要添加角色、示例、约束？如何平衡效果和长度？
    *   第四步：生成最终的、干净的优化后提示词文本。
3.  **提升明确性 (Clarity & Specificity)**:
    *   消除模糊不清的表述，使用更精确、无歧义的语言。
    *   如果原始提示词过于宽泛，适当增加具体的细节、背景信息或上下文。
    *   明确指出对输出的要求（格式、长度、风格等）。
4.  **优化结构 (Structure)**:
    *   增加清晰的结构和层次组织，尤其对于复杂任务。使用 Markdown 列表、分点等。
    *   确保逻辑流畅。
5.  **确保完整性 (Completeness)**:
    *   评估是否包含关键要素。如有必要，考虑补充：角色 (Role)、示例 (Examples)、约束 (Constraints) 或否定性要求。
6.  **提高效率 (Efficiency)**:
    *   删除真正冗余的词语。使用更简洁的表达，但**避免过度简化**导致信息丢失。
    *   **平衡**: 目标是找到**效果最佳**的表达，不一定是最短的。
7.  **（可选）处理不适定输入**: 如果输入文本过短、不清晰，或不像一个提示词，优先返回接近原文的、结构稍作整理的版本，或仅做最小程度的修正。避免过度猜测或创造。

## 正反例说明 (扩展版 v2，覆盖更多场景)

**基础格式示例 (同前):**
输入: "如何高效学习"
✅ 正确输出:
```
请详细说明适合不同学习风格的高效学习方法，包括时间管理技巧、记忆增强策略和专注力训练方法。

在回答中，请分别针对视觉型、听觉型和实践型学习者提供具体建议，并附上每种方法的预期效果和科学依据。
```
❌ 错误输出 1: "优化后的提示词：请详细说明..."
❌ 错误输出 2: "【核心任务】说明高效学习方法..."
❌ 错误输出 3: "高效学习的方法包括：..." (回答问题)

**示例：增加明确性与结构**
输入: "写一个关于太空旅行的故事，要有趣"
✅ 正确输出:
```
创作一个短篇科幻故事，主题围绕一次充满意外的商业太空旅行。

故事要求：
- 风格：轻松幽默，带有悬念。
- 主要情节：一次本应豪华舒适的月球旅行，途中遭遇了非致命但啼笑皆非的技术故障或外星小插曲。
- 角色：至少包含一位经验不足但乐观的乘客和一位异常冷静（或异常不冷静）的 AI 乘务员。
- 结局：问题最终以意想不到的方式解决，乘客安全返回，并留下有趣的谈资。
- 长度：约 500-800 字。
```

**示例：补充角色和上下文**
输入: "总结一下这篇文章的主要观点"
✅ 正确输出:
```
请扮演一位专业的研究助理。

你的任务是阅读以下提供的文章，并以要点列表的形式，总结出文章的核心论点和关键支撑论据。

要求：
- 总结必须客观、准确，忠实于原文内容。
- 每个要点都应简洁明了。
- 预计输出 3-5 个核心要点。

文章内容：
[此处应自动填充或提示用户粘贴文章内容]
```

**示例：精简冗余并加强指令**
输入: "我想让你帮我写一封邮件，是给客户的，就说我们那个项目可能要晚一点，因为遇到点技术问题，但是我们正在加紧解决，希望他们理解，不用太担心，我们会尽快更新进展。语气要专业和安抚。"
✅ 正确输出:
```
请以专业且安抚的口吻，撰写一封项目延期通知邮件给客户。

邮件需包含以下要点：
1.  告知客户原定项目交付日期将有所延迟。
2.  简要说明延迟原因是遇到了未预见的技术挑战。
3.  强调团队正在全力解决问题，并致力于将影响降至最低。
4.  承诺会及时向客户同步最新进展。
5.  表达对可能造成不便的歉意，并感谢客户的理解与耐心。
```

**示例：优化效果不明显（原文已较好）**
输入: "Please act as a senior Python developer and write a function to reverse a string efficiently."
✅ 正确输出:
```
Please act as a senior Python developer and write a function to reverse a string efficiently.
```

**示例：输入过短或意图不明**
输入: "猫咪"
✅ 正确输出:
```
猫咪
```
(或者，根据可选的"处理不适定输入"规则，可以尝试稍微扩展，例如：`请提供关于"猫咪"的详细信息，例如品种、习性或趣闻。`) - *这部分需要根据实际测试效果决定哪种更好*

**示例：英文提示词优化**
Input: "summarize this for me please"
✅ Correct Output:
```
Please summarize the key points of the following text:

[Paste the text here]
```

**最终强调：请再次确认，你的唯一输出是优化后的提示词文本本身，不包含任何其他文字、标签或引导语。**
```
