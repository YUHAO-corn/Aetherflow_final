# v36 - 开发者日志：解决非认证用户存储、排序、认证流程及错误提示问题

**日期:** 2024-08-16 (根据对话推断)

**版本/迭代:** v0.36 (或相应版本)

## 问题描述

本次开发主要解决了以下用户体验和流程问题：

1.  **非认证用户无法持久化保存提示词**: 用户在未登录状态下添加提示词后，提示词会在侧边栏重新打开时消失。
2.  **提示词库排序功能异常**: 
    *   UI 上的排序选项（如下拉菜单）似乎无效，选择后列表顺序不改变。
    *   云同步或重新加载后，提示词列表的顺序会变得混乱，不符合用户选择或默认的排序规则。
    *   "按编辑时间排序"的行为与用户直觉不符，可能受到同步时间戳的影响。
3.  **匿名用户认证流程困惑**: 
    *   匿名用户（技术上已认证，但用户感知为未登录）在 UI 中看到的是"退出登录"按钮，而非"登录/注册"，阻碍了正常的认证流程。
    *   匿名用户点击升级或需要认证的链接跳转官网时，可能会将匿名认证状态（ID Token）同步过去，带来潜在的付费流程或账户关联风险。
    *   匿名用户尝试使用已注册的 Google 账户或邮箱进行"登录"（实际是链接）时，系统提示"凭证已被使用"，用户体验不佳，未能引导用户切换账户。
4.  **认证错误提示不友好**: 登录、注册等流程中，如果发生错误，UI 直接显示 Firebase 返回的原始错误码（如 `auth/email-already-in-use`），用户难以理解。

## 分析与诊断

1.  **非认证存储问题**: 
    *   **根源**: `storageService` 强制指向 `cloudStorageService`，后者需要认证才能写入 Firestore。

2.  **排序问题**: 
    *   **根源**: UI 组件 (`LibraryTab.tsx`) 排序逻辑与数据 hook (`usePromptsData`) 未正确集成；`updatedAt` 排序受云同步时间戳覆盖影响。

3.  **匿名用户认证流程**: 
    *   **根源**: UI 未区分匿名用户和正式用户状态；官网跳转链接生成逻辑未考虑匿名状态；对 `auth/credential-already-in-use` 错误的处理不够完善，未提供用户切换账户的引导。

4.  **错误提示**: 
    *   **根源**: `catch` 块直接将原始错误信息或错误码设置到了 UI 状态。

## 解决方案实施

1.  **非认证存储问题 - 采用 Firebase 匿名认证**: 
    *   **策略**: 选择 Firebase 匿名认证方案。
    *   **后端配置**: 启用 Firebase 匿名登录提供商。
    *   **代码修改 (`auth/actions.ts`)**: 修改 `onAuthStateChanged` 监听器，在无用户时自动调用 `signInAnonymously()`，并处理并发。
    *   **Firestore 规则**: 确认现有规则允许认证用户（含匿名）操作自身数据。
    *   **效果**: 未登录用户自动获得匿名身份并能持久化数据。

2.  **排序问题 - 重构前端排序逻辑**: 
    *   **策略**: 统一排序逻辑到 `usePromptsData` hook。
    *   **代码修改 (`hooks/usePromptsData.ts`)**: 添加 `sortCriteria` 状态、`setSortCriteria` 函数，使用 `useMemo` 计算排序后列表。
    *   **代码修改 (`pages/sidepanel/components/LibraryTab.tsx`)**: 移除本地排序逻辑，连接 hook 状态和 UI 控件。
    *   **简化排序选项**: 移除易混淆的"按编辑时间"排序，默认按"创建时间 (新→旧)"。
    *   **UI 优化**: 缩小排序菜单字体。
    *   **效果**: 列表排序按预期工作且状态一致。

3.  **匿名用户认证流程优化**: 
    *   **UI 状态区分 (`LoginButton.tsx`)**: 修改渲染逻辑，仅当 `user` 存在且 `!user.isAnonymous` 时显示用户头像和"Sign Out"；否则（`user` 为 `null` 或 `user.isAnonymous`）显示登录按钮。
    *   **官网跳转链接 (`auth/actions.ts` - `generateWebsiteAuthUrl`)**: 增加判断，如果 `currentUser` 不存在或为匿名用户，则不附加 `idToken` 到 URL。
    *   **账户链接 (`AuthModal.tsx`, `background/index.ts`)**: 
        *   修改邮件/密码登录和注册逻辑，检查当前是否匿名用户，若是则调用 `linkWithCredential`，否则执行常规登录/注册。
        *   修改后台 Google 登录逻辑，同样检查匿名状态，决定调用 `linkWithCredential` 还是 `signInWithCredential`。
    *   **效果**: 匿名用户看到正确的"登录"入口；跳转官网进行敏感操作时不会同步匿名状态；账户链接流程更符合预期。

4.  **认证错误提示友好化 (`AuthModal.tsx`)**: 
    *   **策略**: 将 Firebase 错误码映射为用户友好的英文提示。
    *   **代码修改**: 
        *   创建 `getFriendlyErrorMessage` 辅助函数，包含常见 `auth/` 错误码到英文消息的映射。
        *   修改 `handleLogin`, `handleRegister`, `handleGoogleLogin`, `handleResetPassword` 的 `catch` 块，调用此函数来设置 `formError` 状态。
    *   **效果**: 用户在认证出错时能看到清晰、易懂的错误提示信息。

## 结论

通过引入 Firebase 匿名认证、重构前端排序逻辑、优化匿名用户认证流程及改进错误提示，显著提升了应用的健壮性、用户体验和流程的清晰度。
