
好的，我们来分别处理这两个问题。

**1. Firestore 安全规则 (问题1)**

你提供的 Firestore 规则看起来在逻辑上应该允许刚刚升级的用户访问他们自己的数据，因为关键的检查是 `request.auth != null && request.auth.uid == userId`，这个条件对于从匿名升级过来的用户（UID 保持不变，`auth` 非空）是满足的。

但是，既然实际测试中出现了权限错误，并且错误似乎发生在匿名用户或刚升级的用户尝试访问 Firestore 时（根据 `log2` 中 `Missing or insufficient permissions` 的日志），我们可以尝试一个稍微调整和简化的规则版本来帮助调试，确保基础的路径访问没有问题。这个版本将明确允许任何已认证用户（包括匿名或刚升级的）访问其 `userId` 下的所有子文档，同时保持对 `membership/status` 的写保护。

我将编辑 `对话记录/firebase rules` 文件应用这个修改后的规则版本。请注意，这稍微放宽了规则（通过 `{document=**}`），主要是为了排查问题。如果这个版本解决了权限问题，我们可以再根据需要细化它。


*   **方案B：智能登录/链接流程 (Smart Login/Link Flow)**
    *   **做法**: 保留启动时的自动匿名登录。修改登录逻辑（比如 `handleLoginWithGoogle`）：
        1.  尝试链接凭证到当前匿名用户 (`linkWithCredential`)。
        2.  如果失败且错误是 `auth/credential-already-in-use`（表示这个 Google/邮箱账户已关联其他用户）：
            *   **核心**: 理解这意味着用户很可能是想用那个已存在的账户登录。
            *   自动或在提示用户后，先**登出**当前的匿名用户 (`auth.signOut()`)。
            *   然后，**重新**发起一次登录，这次使用**标准的登录**流程 (`signInWithCredential`)，而不是链接。
    *   **优点**: 对新用户的无缝体验保持不变。为老用户提供了一条“自动修复”路径，即使他们不小心触发了链接流程。
    *   **缺点**: 实现起来稍微复杂一些，需要精确地处理错误、登出匿名用户、再重新触发登录。需要清晰的提示信息告知用户发生了什么。
